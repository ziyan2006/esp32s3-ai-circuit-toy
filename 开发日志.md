# 开发日志

## 当前状态

- 当前已烧录的是主工程 `tuco_baseboard_firmware`，包含主菜单、关卡树、普通关卡电路搭建与真值表判题、六个特别关卡飞行游戏、硬件调试页，以及 64 端口红外、64 颗连接驱动 WS2812、I2C 积木识别/OLED 和音频诊断等底层功能。红外时序当前为 3200 us 槽位周期、2400 us 稳定等待和 3 次采样，64 端口名义整轮约 4.88 Hz。

## 构建环境与显示

- 开发板：ESP32-P4-Function-EV-Board；实测芯片修订版为 ESP32-P4 v1.3。
- 工具链：ESP-IDF 5.5.3；串口：`/dev/ttyACM0`。
- LCD：1024x600 MIPI-DSI 面板，使用 LVGL 和 ESP LVGL Adapter；状态页面使用垂直 Flex 布局，避免固定坐标越界。
- LCD 帧缓冲依赖 32 MB Hex PSRAM（200 MHz）及 PSRAM 外部内存分配配置。

## 输入、开关与摇杆

- 74HC165 读取链：GPIO4=`PL#`，GPIO6=移位时钟，GPIO22=串行数据；输入低电平有效。
- 74HC165 首字节位定义：SW1..SW4=`bit0..bit3`，Key3=`bit4`，Key2=`bit5`（忽略），Key1=`bit6`，Key0=`bit7`。
- 实际按键定义：左键=Key3，中间键=Key1，右键=Key0。
- Key0、Key1、Key3 具有硬件消抖，软件直接使用采样结果。
- SW1..SW4 具有上拉但无硬件消抖：输入更新约 48 Hz，连续 3 次相同采样才更新稳定状态，确认时间约 62.5 ms；低电平显示为 `ON`。
- 摇杆逻辑映射：上=GPIO36，下=GPIO48，左=GPIO23，右=GPIO54；均为低电平按下，使用板载硬件消抖，不额外进行软件消抖。
- 74HC165 还级联了 48 路红外接收输入。`baseboard_ir` 是 GPIO4、GPIO6、GPIO22 的唯一读取者；输入模块只消费其发布的首字节，禁止并发访问该总线。

## 48 端口红外扫描

- 总线所有权：`baseboard_ir` 独占 74HC595 发射链和 74HC165 回读链。
- 引脚：GPIO21=595 MOSI，GPIO5=595 锁存，GPIO3=595 输出使能，GPIO6=共享移位时钟，GPIO4=165 `PL#`，GPIO22=165 串行数据。
- 发射映射：逻辑端口 `N` 对应 74HC595 的 bit `N`；每个底板模块中 bit0..bit3 对应该槽位端口1..端口4。
- 接收映射：48 路接收数据按六个 8 位组级联输出，必须按组逆序、组内位序不变还原：`logical_port = (5 - raw_bit / 8) * 8 + raw_bit % 8`。
- 每个端口时隙为 4167 us：仅选通一个发射端口，等待 3000 us，读取 3 次完整 `8+48` 位快照并对每位多数表决，随后将 595 输出清零。
- 完整 48 端口扫描约 200 ms，目标频率约 5 Hz。每 5 个端口时隙通知输入任务一次，使输入状态约以 48 Hz 更新。
- 红外接收为高电平有效。扫描同时保存硬件顺序的 `raw_rx[48]` 和映射后的 `logical_rx[48]`。
- 连接规则：忽略端口自身回读；不同端口 A、B 中，检测到 `A->B` 或 `B->A` 任一方向即计为一对无序连接，不重复统计 `A-B` 与 `B-A`。LCD 的 `IR LINKS` 仅在完整扫描结束后刷新。
- 空载基线：`links=0 active_rx=48 overruns=0`。其中 `active_rx=48` 为各端口自身的高有效回读，不表示存在跨端口连接。

## WS2812 连接灯效

- 硬件：GPIO46 驱动底板级联链，共 48 颗；GPIO46 经电平转换和 33 欧串联电阻进入首颗 WS2812 的 `DI`。GPIO2 不属于此 48 灯链。
- 初始化时启用 ESP32-P4 LDO4 的 3.3 V 供电。
- 驱动：ESP-IDF RMT TX，10 MHz，GRB 顺序，MSB first；`0` 位高/低=3/9 ticks，`1` 位高/低=9/3 ticks；一帧为 48 * 3 = 144 字节。
- 红外每完成一轮完整扫描后通知 WS2812 任务。该任务读取连接矩阵并发送一帧；红外扫描任务不等待 RMT 发送完成。
- 逻辑端口 `N` 当前顺序对应 WS2812 LED `N`。
- 每个连接对的两个端口显示相同颜色；不同连接对分配不同颜色；未连接端口关闭。
- 调色板支持最多 24 对不重叠连接。若某连接对在相邻完整扫描中保持存在，则复用上一轮颜色；新连接对从未占用颜色中分配。
- 若同一端口在同一轮中被判定属于多个连接对，按排序后的第一对渲染，并记录重叠诊断，避免同一灯珠被重复写入不同颜色。

## 独立测试工程备份

- `test_backups/ws2812_48led`：GPIO46、LDO4、48 颗 WS2812 与独立红色跑马灯测试。
- `test_backups/input_lcd`：74HC165 按键/SW、四方向摇杆、PSRAM、MIPI-DSI LCD 与实时状态显示；不包含红外或 WS2812。
- `test_backups/ir_ws2812_lcd`：48 端口红外扫描、连接对 WS2812 灯效、输入检测和 LCD `IR LINKS` 显示。
- `test_backups/i2c_oled`：74HC165 输入读取、四方向摇杆、本地 EEPROM 编程、两颗 TCA9548A 的 12 槽位扫描，以及 SSD1315 OLED 显示；不包含红外、WS2812 或主 LCD。
- `test_backups/audio_self_test`：ES8311 扬声器 1 kHz 输出和麦克风同频相关检测；不包含红外、WS2812、LCD 或积木 I2C。
- `test_backups/full_hardware_bringup`：当前主工程完整硬件基线，包含 48 端口红外、WS2812、74HC165 输入、摇杆、LCD、双 I2C/EEPROM/TCA/SSD1315 OLED，以及 ES8311 音频自检。
- 六个工程均可独立执行 `idf.py set-target esp32p4`（首次使用或切换目标时）以及 `idf.py -p /dev/ttyACM0 build flash` 构建和烧录。
- 前四个备份工程均已完成独立构建和烧录，bootloader、分区表和应用镜像写后哈希校验通过。`audio_self_test` 和 `full_hardware_bringup` 均已完成独立构建；板上保留主工程音频自检固件。

## 最近验证

- `ir_ws2812_lcd` 启动时已确认 LCD 状态界面、48 端口红外扫描和 WS2812 连接灯效任务均已启动。
- 最近一次串口运行状态为 `IR links=5 rendered_pairs=5 overlap=0`、`overruns=0`。该数字反映当时接入状态；连接数随实际硬件连接关系变化。

## I2C 积木识别与 OLED 显示

- 源文件：`main/block_i2c.c` 管理两条 I2C 总线、EEPROM 与 TCA9548A；`main/ssd1315_oled.c` 负责 SSD1315 0.96 英寸、128x64 单色 OLED 的初始化及整帧绘制。独立备份 `test_backups/i2c_oled` 复制同一套模块，并自行读取 74HC165 的首字节和摇杆输入。
- 本地编程总线：I2C0，GPIO32=SDA、GPIO33=SCL，400 kHz，启用内部上拉。EEPROM 支持 7 位地址 `0x50` 和 `0x51`，积木类型 ID 固定存放在偏移 `0x00`；本地 OLED 支持地址 `0x3C` 和 `0x3D`。
- 本地编程仅在 EEPROM ID 可读且 SSD1315 OLED 可访问时允许输入控制。未插入时每 25 ms 搜索一次，确认插入后每 100 ms 健康检查；连续 2 次完整采样确认插入，连续 3 次不完整采样确认拔出。左键 Key3 或摇杆左选择上一种类型，右键 Key0 或摇杆右选择下一种类型，Key1 中键确认写入。输入在约 48 Hz 的更新点按按下边沿入队，队列深度为 8；插入时已按住的按键不会产生写入，需先松开再按下。
- 合法 ID 映射为：`0xF0=INPUT`、`0xF1=OUTPUT`、`4=NOT`、`5=AND`、`6=OR`、`7=NAND`、`8=NOR`、`9=XOR`、`10=XNOR`。读到未知 ID 时只将当前选择置为 `INPUT`，不会自动改写 EEPROM，等待用户确认。
- 确认写入顺序为：发送 `[0x00, ID]`，每 2 ms 进行一次 ACK 轮询（最多 10 次），然后读回偏移 `0x00` 验证值一致；仅验证成功才显示成功状态。OLED 成功页居中显示“成功修改！”，持续 1.5 秒。字模来自 `ssd1315_success_font_24.h` 的 Noto Sans SC 24x24 子集，只包含成、功、修、改、！五个字模（共 360 字节），运行时不依赖中文字库。
- 底板扩展总线复用 LCD BSP 已建立的 I2C1：GPIO7=SDA、GPIO8=SCL，固定 100 kHz。GPIO47 是两颗 TCA9548A 的低有效复位脚，初始化后保持高电平。一次槽位会话从 TCA 选通到两颗 TCA 全部关闭均持有底板互斥锁和 LVGL adapter 锁，避免同总线 GT911 触摸读取在通道打开期间插入；底板器件不得提高到 400 kHz。
- 12 个槽位通过 `main/board_mapping.c` 的显式表选择，槽位编号采用从拓展板沿级联信号顺序的 1..12：槽位1/2=`0x70 CH0/CH1`，槽位3/4=`0x70 CH2/CH3`，槽位5/6=`0x70 CH4/CH5`；实测下半区线缆方向反向，槽位7/8=`0x71 CH4/CH5`，槽位9/10=`0x71 CH2/CH3`，槽位11/12=`0x71 CH0/CH1`。访问时只开启目标通道并关闭另一颗 TCA；完成后关闭全部通道。槽位先确认 EEPROM `0x50` 的偏移 `0x00` 可读，再探测 OLED `0x3C`/`0x3D` 并写屏，因此空槽位不会收到 OLED 写入。
- SSD1315 对合法 ID 在屏幕上部绘制 INPUT、OUTPUT 或七种逻辑门图案，底部显示对应英文名；EEPROM 可读但 ID 非法时显示 `NULL`。整帧先设置一次 128x64 窗口，再以 8 个 128 字节数据事务发送，共 9 个事务；本地总线实测缓存初始化后的整帧约 26 ms，底板 100 kHz 整帧约 100～107 ms。
- 本地编程任务优先级 4，只负责检测、输入和 EEPROM 写入；OLED 由优先级 2 的独立任务刷新。长度为 1 的刷新队列始终保存最新画面，连续选择操作使用 35 ms 合并窗口，因此旧帧不会逐个阻塞输入处理。
- 底板身份扫描任务优先级 2，每槽 30 ms，稳定完整轮询约 360 ms；身份结果按槽位确认后立即发布，不等待整轮结束。新插入或 ID 变化必须连续 2 次读到相同 ID 才生效，单次异常值不会改变 LCD 电路或触发 OLED；连续 3 次读取失败才确认拔出。优先级 1 的独立 OLED 任务仅在身份确认变化、首次显示或失败重试时刷新，不阻塞其他槽位的身份发布。I2C 模块不读写红外的 74HC595/165 总线。
- 最终独立验证：`test_backups/i2c_oled` 已于 2026-07-16 使用 ESP-IDF 5.5.3 为 ESP32-P4 v1.3 完整构建并烧录至 `/dev/ttyACM0`。应用镜像 `0x4A6E0`，1 MiB 分区剩余 `0xB5920`（71%）；bootloader、分区表、应用镜像写后哈希校验均通过。启动串口确认两条 I2C 总线就绪，并读到本地 EEPROM `id=0x0A`、当前选择 `XNOR`。
- 2026-07-17 主工程优化实测：本地积木读取为 `id=0x0A/XNOR`，本地 OLED 首帧约 26 ms；底板槽位11稳定为 `0xF1/OUTPUT`、槽位12稳定为 `0xF0/INPUT`，完整身份扫描稳定约 360 ms。自动游玩压力测试超过 80 秒、完成 200 轮以上底板身份扫描，未出现 `lcd_panel.io.i2c`、GT911、ID 跳变、看门狗或异常复位；同期红外保持约 5 Hz 且 `overruns=0`。正式主菜单固件镜像为 `0xFA010`，应用分区剩余 `0xF5FF0`（约 50%），已烧录至 ESP32-P4 v1.3 并完成写后哈希校验；恢复主菜单启动后继续观察约 65 秒无异常。

## 音频自检

- 主工程新增 `main/audio_self_test.c`。板载音频编解码器为 ES8311：I2S1 使用 GPIO12=BCLK、GPIO13=MCLK、GPIO10=LRCK、GPIO9=DOUT、GPIO11=DIN；GPIO53 为 BSP 管理的功放使能。编解码器控制与底板扩展共用 BSP I2C1（GPIO7/8），因此音频在底板 TCA 轮询任务创建前完成初始化，后续测试只访问 I2S 数据通道。
- 测试配置为 16 kHz、16 bit、单声道全双工。扬声器任务连续输出 1 kHz 正弦测试音，音量设为 70；麦克风任务连续读取 256 个采样点，每 32 个块（约 0.5 秒）计算 RMS 和 1 kHz 正交相关比例。`RMS >= 120` 且相关比例 `>= 0.12` 时输出 `DETECTED`。
- 此检测并非只判断环境音量：正交相关同时比较 1 kHz 的正弦与余弦分量，能区分扬声器测试音和非同频噪声。扬声器写入和麦克风采集运行于两个优先级 3 的 FreeRTOS 任务。
- 2026-07-16 已完成主工程构建和烧录到 `/dev/ttyACM0`（ESP32-P4 v1.3），bootloader、分区表和应用镜像写后哈希校验均通过。串口连续记录 `mic rms=1028.0..1099.4`、`tone_1000Hz=0.969..0.999`、`DETECTED`；同期红外扫描 `overruns=0`。因此本板扬声器测试音和麦克风接收链路均已跑通。
- 音频独立备份位于 `test_backups/audio_self_test`，仅保留 `audio_self_test.c/.h` 和最小启动入口。2026-07-16 已使用 ESP-IDF 5.5.3 为 ESP32-P4 独立构建，镜像为 `0x64C00`，1 MiB 应用分区剩余 `0x9B400`（61%）；未烧录该备份，避免覆盖当前已验证的主工程固件。
- 完整硬件基线快照位于 `test_backups/full_hardware_bringup`。该目录保存当前主工程源码、`sdkconfig.defaults`、实际 `sdkconfig`、依赖锁定文件与 OLED 字模生成工具；不以生成的构建目录或组件目录作为源码基线。2026-07-16 已在该目录独立构建通过，镜像为 `0xCDDA0`，1 MiB 应用分区剩余 `0x32260`（20%）；未烧录快照，当前板上仍为主工程同版本固件。

## 应用分区扩展

- 2026-07-16 主工程新增根目录 `partitions.csv`，并在 `sdkconfig`、`sdkconfig.defaults` 启用自定义分区表。仅调整 Flash 分区，不修改任何硬件驱动、任务或业务逻辑。
- Flash 仍为 2 MiB。保留原有 `nvs`（`0x9000`，24 KiB）和 `phy_init`（`0xF000`，4 KiB）；`factory` 应用分区从默认 1 MiB 扩展为 `0x10000..0x200000`，即 `0x1F0000`（1984 KiB）。当前不使用 OTA 分区。
- 本次主工程镜像为 `0xCDDA0`（843168 B），分区校验结果为剩余 `0x122260`（约 1.13 MiB，58%）；这为后续关卡 UI 和精简中文字体保留了充足空间。
- 已使用 ESP-IDF 5.5.3 烧录至 `/dev/ttyACM0`，目标为 ESP32-P4 v1.3；bootloader、应用镜像和分区表均完成写后哈希校验。串口运行验证显示红外扫描持续约 5 Hz、`overruns=0`，WS2812 连接帧持续更新，且音频自检保持 `DETECTED`，确认扩展分区后原有固件正常运行。

## 主业务 UI：全新科幻界面架构

- 本次从 `main/app_ui.c` 开始完全重写 LVGL UI。除关卡数据源 `main/campaign_content.c/.h`（17 个普通关卡的名称、坐标、前置关系、剧情和解锁文案）及既有设置/调试业务行为外，不复用旧项目的页面层级、样式、触摸拖拽或生命周期代码。
- 交互结构参考 LVGL 官方 GitHub 的 grid navigation、menu sub-page 和 event-driven 示例（`https://raw.githubusercontent.com/lvgl/lvgl/master/examples/others/gridnav/lv_example_gridnav_1.c`、`https://raw.githubusercontent.com/lvgl/lvgl/master/examples/widgets/menu/lv_example_menu_sub_page.c`）：主菜单、关卡航线、关卡详情、普通关卡游玩、成功页、设置和硬件调试共 7 个 screen 在初始化时一次性创建，运行时使用 `lv_screen_load()` 切换；特别关卡飞行页因对象池较大，进入时动态创建、退出后异步删除。触摸事件中不重建常驻页面，也不使用 `lv_async_call()`。
- 触摸事件只处理点击：主菜单卡片、设置项、返回按钮、地图中当前可见节点和详情入口均使用 `LV_EVENT_CLICKED`。地图与普通面板关闭滚动；地图没有拖拽和缩放。详情文本使用一个标准 LVGL 纵向滚动容器。
- 第三版视觉面向儿童重新设计，参考 `nexu-io/open-design` 的 `html-ppt-zhangzara-8-bit-orbit` 模板：深海蓝背景、青色/粉色/黄色像素强调、星点背景、方角像素边框和带眼睛嘴巴的圆形小机器人图案。主页只保留 `关卡游玩` 和 `设置` 两个中文入口；删除 IR BUS、I2C、SYSTEM CORE、ROUTE MATRIX 等开发人员装饰文字，页面文字只用于说明关卡、引导选择或显示必要诊断状态。设置、关卡地图、任务详情和调试页全部改为中文。
- 上电显示左右排列的 `关卡游玩`、`设置` 两个模块。主页摇杆左选择“关卡游玩”、摇杆右选择“设置”，Key1（中键）确认；主菜单触摸点击直接进入对应模块。Key3（左键）和每页左上角返回按钮执行返回：详情回关卡航线，其余子页回主菜单。Key0（右键）在设置页执行当前选中项目，不承担页面导航。
- 关卡航线使用 3400x1000 世界坐标，但显示为 724x500 的固定视口。17 个普通关卡和 6 个特别关卡节点及其前置连线只创建一次；摇杆方向选择同方向最近节点，并将其重定位到视口中心，视口外节点隐藏。进度只存在 RAM，初始仅 101 可进入，完成关卡后按前置关系开放后续节点；锁定节点仍可查看详情，但不能进入玩法。Key1、可见节点点击或详情入口进入关卡详情。
- 关卡详情保留 corrected 的完整剧情/目标文案，使用标准纵向滚动文本区；普通关卡进入电路搭建和真值表判题，特别关卡直接进入对应飞行挑战。设置页当前只有 `扬声器测试` 和 `解锁所有关卡` 两个可选项目。
- 选中“扬声器测试”时按住 Key0（右键）输出 1 kHz，松开、切换选项或离开设置页立即静音；选中“解锁所有关卡”后按一次 Key0，会在本次上电期间开放全部节点但不标记完成。原硬件调试 screen 仍保留在固件中供内部诊断，不再由设置菜单进入；麦克风采集持续运行。
- GT911 仍由 ESP LVGL Adapter 注册。来自输入任务的 `app_ui_update()` 在 adapter 互斥锁内更新状态和焦点；触摸回调运行于 LVGL 线程，使用同一组预创建对象，避免跨线程操作和悬空对象。
- `main/app_ui_font_cn_16.c` 和 `main/app_ui_font_cn_24.c` 均按当前 `app_ui.c` 与 `campaign_content.c` 的实际字符串生成：16 px 字库保留当前关卡正文、中文标点和必要 ASCII，24 px 字库只保留页面标题、主页标题、普通关卡名及“奖励关”。字体源均为 `managed_components/lvgl__lvgl/scripts/built_in_font/SourceHanSansSC-Normal.otf`。
- 星点和像素边框使用 LVGL 绘制回调实现，不为每个装饰图形创建独立对象；这保持了儿童版图案风格，同时控制 LVGL 对象数量和内存占用。未使用渐变、透明大叠层或模糊阴影。
- 2026-07-17 使用 ESP-IDF 5.5.3 为 ESP32-P4 v1.3 完成儿童版 UI 精修构建：应用镜像 `0xEEFD0`，`0x1F0000` 应用分区剩余 `0x101030`（约 52%）。右上角状态胶囊改为固定单行居中布局，关卡地图删除无关的“全部关卡都能玩”提示。未改变红外、WS2812、输入、I2C/OLED 或音频硬件任务。
- 最新镜像已烧录到 `/dev/ttyACM0`，bootloader、分区表和应用镜像写后哈希校验通过。烧录后串口连续运行 30 秒以上：红外扫描保持约 5 Hz、`overruns=0`，WS2812 每轮扫描后更新，麦克风持续采样，未出现新的异常重启。触摸点击、返回、摇杆切换和详情滚动仍需在实机屏幕上进行人工交互验证。
- 2026-07-17 本机使用 LVGL 9.4 软件绘制器按 1024x600 实际分辨率离屏渲染右上角状态胶囊，对比发现 `status_pill` 未清零默认内边距，导致圆点和“准备好了”文字整体下移并被绿色边框裁剪。已在 `main/app_ui.c` 为该容器设置 `pad_all=0`，保留固定位置、36 px 高度、单行裁剪与居中对齐；修复后离屏画面中圆点和文字完整显示，未改动其他页面或硬件任务。
- 2026-07-17 关卡详情页的“可进入”状态胶囊存在同类默认内边距问题，已为 `available` 容器设置 `pad_all=0`，使状态圆点和文字按可视胶囊边界对齐，未改变关卡数据或详情交互逻辑。
- 2026-07-17 关卡树右侧当前选择摘要改为只显示 `【遭遇事件】` 和 `【行动】` 两段，移除中间的 `【晶体挑战】` 段以避免摘要超出面板；中键进入的关卡详情仍直接显示完整 `node->goal` 原文。
- 2026-07-17 修正关卡树分支导航：方向选择加入 5:4 的主方向锥限制，斜向的汇合节点不再抢占上下左右操作。例如上方分支按下会先选择下方分支，不会直接跳过分支节点到汇合点。

## 特别关卡与固定成长路线

- 2026-07-17 按百位关卡组在 `103、203、302、403、504、602` 后插入 `150、250、350、450、550、650` 六个特别关卡并接入前置路线。特别关卡使用紫色底、粉色双层方角边框、黄色文字和像素角标，与普通关卡明确区分。
- 六个特别关卡已分别接入非门熔炉、加法器航道、与门堡垒、奇偶回路、异或反应堆、与门要塞。飞船的核心、推进、子弹伤害、射击间隔和并行弹道按普通关卡路线固定成长，不提供技能树或自由配点。
- 关卡树扩展为 23 个常驻节点后，LVGL 内置堆最终在 `sdkconfig` 和 `sdkconfig.defaults` 配置为 192 KiB（`CONFIG_LV_MEM_SIZE_KILOBYTES=192`），同时容纳常驻业务页面和动态飞行页。`app_ui_init()` 启动日志会输出 LVGL 占用率、空闲字节、最大连续块和碎片率，便于继续扩展时检查容量。
- 2026-07-17 使用 ESP-IDF 5.5.3 完整构建并烧录到 `/dev/ttyACM0`（ESP32-P4 v1.3）；应用镜像为 `0xEF930`，`0x1F0000` 应用分区剩余 `0x1006D0`（52%），各镜像写后哈希校验通过。实机启动创建 23 个关卡节点后，LVGL 内存占用 49%、空闲 63720 B、最大连续块 63016 B、碎片率 2%；串口持续观察至红外第 60 轮扫描，保持约 5 Hz、`overruns=0`，WS2812 持续更新，未出现看门狗、内存断言、异常重启或崩溃。

## 关卡游玩、实时电路与真值表判题

- 2026-07-17 新增关卡游玩页和成功页，LVGL 由 5 个扩展为 7 个常驻 screen。标准关卡从详情页按中键或触摸“开始组装”进入；游玩页左键或触摸返回直接回关卡树；判题成功后停留在独立成功画面，成功画面返回游玩页，方便继续尝试其他电路。特别关卡不进入电路游玩，而是从详情页进入对应飞行挑战。
- 新增 `play_mode` 运行模式。非游玩页面保持 595 全零、48 路红外发射关闭、GPIO46 的 48 颗 WS2812 全灭，两条积木 I2C 任务常驻但不访问器件；165 仍约每 20 ms 独立回读一次，以维持 KEY 和 SW 输入。进入游玩页后恢复约 5 Hz 红外全扫描、WS2812 连接反馈、本地积木编程器和底板积木轮询；退出时取消判题、清空快照、关闭发射和 TCA 通道并清灯。
- 本地 I2C 积木只在游玩模式检测。一次有效在位采样必须同时满足 EEPROM 应答、ID 读取成功以及 `0x3C/0x3D` 的 SSD1315 OLED 应答；搜索阶段以 25 ms 周期连续 2 次有效后确认插入，在位后以 100 ms 周期连续 3 次不完整采样确认拔出，避免总线瞬态应答让“摇杆和按键正在修改本地积木”误闪。确认插入后，Key3/Key0、Key1 和摇杆输入只交给 EEPROM ID 修改，不再同时驱动 LCD；触摸返回仍可使用。退出游玩模式会清空采样计数、编程命令和输入独占状态。底板 I2C 固定为 100 kHz、每槽 30 ms，12 槽稳定轮询约 2.8 Hz；OLED 只在确认插入、确认 ID 变化或失败重试时重绘。
- 新增 `board_snapshot` 作为唯一硬件状态源，统一保存 12 槽身份、48 端口角色、红外完整扫描、一对一连接、合法性、稳定颜色、I2C/IR轮次和拓扑修订号。LCD 电路图、WS2812 和判题均读取同一快照；任务只发布快照，不直接操作 LVGL。相同连接沿用上一轮颜色；方向错误、多重连接或积木身份未知的错误接线使用红色反馈，未定义物理端口连接使用普通配对颜色但不进入电路图和判题。
- 最终端口语义以 `积木映射表` 为准：NOT 为左输入、右输出；INPUT 四个端口均为等价输出；OUTPUT 仅左端口为输入；AND/OR/NAND/NOR/XOR/XNOR 为上、下两个输入和右输出。底板上方槽位的物理端口方向为上=P3、下=P1、左=P2、右=P0；下方槽位为上=P5、下=P7、左=P4、右=P6。未定义物理端口的连接保留普通 WS2812 配对颜色，但不绘制 LCD 连线，也不参与判题；输入接输入、输出接输出和同一物理端口多重连接仍属于错误。
- 游玩页保持现有儿童科幻风格：左侧 `660x492` 为单一自绘电路视图，最多显示 12 个积木，按输入/中间门/输出分层布局并自动居中；门图标、端口、正交线路和跨线底色均在绘制事件中完成，不按扫描轮次创建或删除 LVGL 对象。右侧严格复用详情状态栏的 `312x492` 尺寸，显示关卡目标、输入输出命名、真值表、检查状态和唯一操作“启动飞船检查”。
- 2026-07-17 实时电路布线改为门体避障路由。每个端口先沿门的外侧离开，再进入相邻列间走廊；两端位于同一走廊时直接纵向连接，不同走廊时从全部门体的上方或下方绕行。不同连接按索引分配 5 个纵向走廊偏移和 4 个外侧横向偏移，继续尽量减少重线；同一积木不同端口之间的连接也使用同一算法，不再采用可能穿过相邻门的固定底部回路。
- 路由对门边保留 8 px 间距，能够容纳 7 px 的深色跨线底层。每条路径最多 6 个正交点，生成后逐段检查是否进入任一门体内部；校验失败时不绘制该路径。启动自检使用 3 个输入、3 个输出和 6 个中间门组成 12 节点布局，穷举所有节点对及四端口组合，覆盖同门、同列、相邻列和跨列路径。
- 新增17个普通关卡的可执行规则，系统自动枚举全部输入组合，只按最终真值表功能判定，不限制门种类、数量或具体拓扑。规则依次为：101直连、102 NAND、103 NOT、201 AND、202 OR、203 NOR、301 XOR、302 XNOR、401求和、402进位、403半加器、501三输入奇偶、502三输入多数进位、503多路OR、504全加器、601二选一MUX、602二转四译码器。外部 INPUT/OUTPUT 数量必须与当前规则一致。
- 选中“启动飞船检查”并按中键或触摸后，真值表按行执行，每行保持 400 ms。判题期间继续监视拓扑修订号；积木或接线改变会立即取消并提示重新检查。全部通过后在 RAM 中记录本次上电已完成，并在关卡树使用绿色边框标记；当前仍不保存到 Flash。
- 真值表右侧改为 LVGL 自绘区域：输入、目标和实际结果中的 `1` 用绿色圆点、`0` 用红色圆点表示；未计算的实际结果显示为灰色空心提示。判题时当前行使用青色背景高亮，该行计算完成后立即显示当前电路实际输出，随后保持 400 ms 再检查下一行；已完成行保留实际输出和通过/未通过标记。
- 修复在积木不变时切换关卡仍需等待数秒的问题。退出游玩模式不再清空已确认的槽位身份、门类型和 OLED 已显示状态，只清理采样计数与未执行刷新请求；再次进入后约一个任务调度周期内先把缓存身份发布到 `board_snapshot`，LCD 可直接恢复实时电路，后台再按原有连续 2 次 ID 确认、连续 3 次失败移除规则重新校验。只有身份实际变化、首次显示或上次刷新失败的积木才重写 OLED。
- 底板身份扫描任务优先级由 2 提高到 5，避免被约 48 Hz 的 UI 更新及 LVGL 触摸轮询长期饿死；短 EEPROM/TCA 会话仍持有共享总线和 LVGL 锁，OLED 慢刷新仍在独立低优先级任务中执行。`ui_update` 任务栈由 4096 B 增加到 8192 B；修改前复现过程测得历史最小余量仅 212 B，这同时消除了连续判题/页面切换的潜在栈溢出重启风险。
- 新增启动自检，覆盖槽位/TCA/端口方向映射唯一性、17关规则存在性与关键真值表、最新端口语义下的直连求值、NAND/NOT实际网表求值、未定义端口忽略策略、12槽布局边界及门体避障路由。2026-07-17 使用 ESP-IDF 5.5.3 完整构建并烧录到 `/dev/ttyACM0`，最新应用镜像 `0xFA9E0`，`0x1F0000` 分区剩余 `0xF5620`（约 49%），bootloader、应用和分区表写后哈希校验均通过。上一版串口确认 ESP32-P4 v1.3、映射自检、17关自检、路由穷举自检、7个常驻页面和主菜单正常启动；本次按要求停止持续串口监视，关卡切换缓存响应由实机交互确认。
- 游玩页刷新改为按状态语义触发：IR/I2C 每轮仍推进快照代数，但只有积木或接线实际变化导致 `topology_revision` 改变时才重新布局并重绘电路；右侧文字只在首次进入、判题状态或本地编程器输入所有权改变时更新，避免每轮扫描反复执行 LVGL 标签内存分配。串口中 WS2812 帧只打印前3帧及每25帧，连接统计只在数量变化时打印，减少长期运行日志压力。
- 游玩页每30秒打印一次 `play health`，包含 LVGL 已用比例、空闲字节、最大连续块、碎片率、`lv_mem_test()` 完整性和 UI 更新任务栈余量。实机进入游玩页后观察到7组、首末跨度240秒的数据均为：LVGL占用58%、空闲52076 B、最大连续块51252 B、碎片率2%、完整性`ok`、UI任务栈余量1404 B；同期红外完成1280轮全扫描、WS2812输出至第1275帧、`overruns=0`，没有看门狗、异常复位或崩溃，且内存和任务栈余量没有随扫描轮次下降。
- 当前一对一连接模型仍不允许同一个物理端口同时连接多根线。INPUT 和 OUTPUT 按最终左右端口规则各只有一个逻辑端子；中间逻辑门输出也只有一个端子，403半加器、502多数进位、504全加器、601 MUX、602译码器等需要复用中间信号的组合电路，在通用分线算法加入前仍可能无法实际通关。
- 2026-07-17 修正游玩页物理连接反馈：新增统一槽位/端口映射，I2C身份、红外连接、WS2812灯珠和LCD节点使用同一套编号；下半区`0x71`反向线缆按实测槽位11/12=`CH0/CH1`处理。未定义端口连接改为普通颜色并计入ignored，不再误报红色或阻断判题；LCD仍只绘制两端均为逻辑端子的线路。主工程使用 ESP-IDF 5.5.3 构建并烧录到 ESP32-P4 v1.3，镜像 `0xF9250`，应用分区剩余 `0xF6DB0`（50%），写后哈希校验通过。

## 项目文档

- `项目介绍.md` 用于对外概述项目定位、当前功能、基本游玩流程和未来规划。云端 AI 语音提示、云端游玩数据同步及钮子开关实时调试仍属于产品规划；特别关卡飞行游戏现已加入主工程。
- `现场演示方案/5分钟现场演示方案.tex` 与同名 PDF 是竞赛现场演示的两页内部排练稿，包含逐段时间轴、AI 演示台词、创新点、奖励关展示、现场准备和备用方案；PDF 使用 A4 黑白简洁版式，供参赛成员和指导老师批注。
- `shooter_simulator` 使用条件编译同时保留 Windows Win32 后端和 Linux 原生 X11 后端；Linux 的 `esp_timer_get_time()` 使用单调时钟，窗口关闭采用 X11 直接退出。Linux 一键入口为 `shooter_simulator/run_simulator.sh`，构建产物放在独立的 `build-linux` 目录，不影响原有 Windows `build`。当前玩法和按键以紧随其后的精简版本记录为准。

## 打飞机 Linux 原型精简与固定成长

- 2026-07-17 仅重构 `shooter_simulator`，未修改主工程 `main/`、ESP-IDF 配置、分区或嵌入式固件。删除序章、生存计时、陨石资源、技能树、技能点、晶体选择、六种旧大招及其被动状态；模拟器中未被飞行游戏使用的逻辑判题副本 `src/content`、`src/logic` 和 `src/hw/feedback_bus` 也已移除。
- 原型固定为六个飞行挑战：非门熔炉、加法器航道、与门堡垒、奇偶回路、异或反应堆、与门要塞。核心数依次为 `3/3/3/3/4/5`，推进等级为 `0/1/1/2/2/2`，普通子弹伤害为 `1/2/2/2/4/4`，射击间隔为 `320/320/240/180/180/180 ms`，每次平行发射数为 `1/1/2/2/3/4`。所有数值集中在 `src/shooter_stage.c` 的固定配置表中。
- 射击改为手动边沿触发：Linux/Windows 均使用 `J`，长按只发射一次；冷却期内的按键不排队，冷却结束后必须重新按下。Linux 使用 `K` 启动主动装置；嵌入式版对应中键发射、右键启动装置。PC 原型仍保留数字键 `1..6` 快速切关和 `ESC` 返回选择页，嵌入式版不包含飞行关卡选择页。
- 第1关没有主动装置；第2、3关解锁星光护盾；第4至6关护盾与贯星能量炮绑定释放。装置冷却固定10秒，护盾持续3秒并通过统一伤害入口免疫敌机碰撞、敌方子弹、激光和首领场地伤害。能量炮持续1秒、宽240逻辑像素，伤害为当前普通子弹伤害的3倍；每个敌人记录本次光炮编号，同一次光炮只受伤一次，新生成敌人仍可被当前光炮命中。
- PC首页重写为六张固定关卡卡片，不再提供任何配点或装备入口；战斗HUD只显示核心、装置冷却、波次、状态、普通子弹伤害和连发数。16 px中文字体按当前模拟器源码实际文案重新生成，字体源为 `shooter_simulator/lvgl/scripts/built_in_font/SourceHanSansSC-Normal.otf`，生成后的 `app_ui_font_cn_16.c` 约239 KiB。
- 精简后关键源码 `preview/main.c`、核心、渲染器、页面控制器和自检共约2987行，精简前约5420行。Release可执行文件为2398632 B；其中仍包含LVGL和飞船、敌机、首领、星球等实际使用的位图资源。
- 核心自检覆盖六关配置、无序章回退、手动发射边沿、冷却期丢弃输入、多发数量、伤害、推进速度、3秒护盾、10秒装置冷却、光炮三倍伤害和单次命中。Release自检与Debug `ctest` 均通过。1024x600 X11实测确认：首页无缺字或溢出，第1关不显示装置，第4关同时绘制护盾和光炮，第6关一次显示4条平行弹道，键盘和鼠标按钮均可用。连续40轮进入/退出不同关卡后窗口正常，RSS在首次对象池分配后稳定为8476 KiB，后20轮未继续增长。

## 特别关卡飞行游戏嵌入式移植

- 2026-07-17 主工程复用 `shooter_simulator/src` 中已精简并自检通过的纯游戏核心、六关配置、波次和 LVGL 渲染器；嵌入式适配集中在 `main/shooter_game.c/.h`。关卡树直接映射 `150→第1关非门熔炉`、`250→第2关加法器航道`、`350→第3关与门堡垒`、`450→第4关奇偶回路`、`550→第5关异或反应堆`、`650→第6关与门要塞`，不创建飞行游戏内部选关页。
- 特别关卡先进入原有详情页，在详情页按 Key1 中键或触摸“开始飞行”直接进入对应挑战。摇杆四方向控制飞船，Key1 中键手动发射，Key0 右键启动当前关卡已解锁的装置；Key3 左键或触摸左上角返回直接退出并回关卡树。胜负结算后中键可重试，挑战成功会在本次上电期间标记该特别关卡已完成。
- 飞行页进入时动态创建固定容量的星点、子弹、敌机、爆炸和 HUD 对象池，退出时先停止 16 ms LVGL 定时器，再切回关卡树并异步删除整个飞行 screen。飞行期间 `play_mode` 保持关闭，因此红外发射、WS2812 和两条积木 I2C 不启动游玩轮询；74HC165 按键/开关回读和摇杆输入继续工作。
- 原型 ARGB8888 位图不直接写入固件。`tools/generate_shooter_assets.py` 将飞船、敌机、子弹、首领和实际使用的 6 张星球图转换为 LVGL `RGB565A8` 二进制资源，输出到 `main/assets/shooter/`，资源总计 555555 B；透明度单独保留，已通过本机重建图像检查颜色与透明区域。嵌入式飞行文案使用独立的 `main/shooter_font_cn_16.c` 中文 16 px 子集字体。
- 嵌入式启动时在临时 8192 B FreeRTOS 任务中运行六关核心自检，完成后通知主任务并自动删除，避免占用 IDF 主任务栈。UI 初始化还会连续执行两轮飞行页创建/销毁自检并调用 `lv_mem_test()`：首次预热后 LVGL 空闲 119460 B，第二轮仍为 119460 B，最大连续块 119372 B、碎片率 1%；飞行页存在时空闲 92964 B、最大连续块 92916 B，未发现逐次进入造成的泄漏。
- 最终使用 ESP-IDF 5.5.3 完整构建并烧录到 `/dev/ttyACM0`，实测目标为 ESP32-P4 v1.3。应用镜像为 `0x193280`，`0x1F0000` 应用分区剩余 `0x5CD80`（约 371 KiB，19%）；bootloader、分区表和应用镜像写后哈希校验通过。板载 Flash 实际容量为 16 MiB，但当前配置仍按 2 MiB 使用。烧录后串口持续观察约 75 秒，核心与生命周期自检、既有 17 关规则/映射/布局自检、LCD、触摸、音频和 I2C 均正常启动，未出现看门狗、异常复位或崩溃；飞船移动、发射、装置、结算和画面流畅度仍需实机人工操作确认。
- 2026-07-17 实机确认 16 ms 刷新节奏和当前对象池下的画面流畅度满足使用要求，后续保持该刷新方案。修正移植时对三张自定义敌机贴图的额外缩小：红色激光精英恢复为原生 `48x48`，自定义精英恢复为 `72x72`，Boss 恢复为 `192x112`；只在敌人槽位切换类型时设置图片源和缩放，不增加每帧分配或对象数量。
- 同次平衡调整将自定义精英的基础血量从前期/后期 `3/5` 提高到 `4/7`，经过统一三倍敌人血量系数后，实际最大血量为 `12/21`；第六关“与门要塞”Boss 基础血量从 `22` 提高到 `28`，实际最大血量从 `66` 提高到 `84`，第五关 Boss 保持 `54`。启动核心自检新增前期精英、后期精英和第六关 Boss 的实际生成血量断言，防止后续配置回退。
- Linux 原型重新构建后核心自检通过；ESP-IDF 5.5.3 主工程完整构建并烧录到 `/dev/ttyACM0`（ESP32-P4 v1.3），应用镜像 `0x193600`，`0x1F0000` 分区剩余 `0x5CA00`（19%），三段镜像写后哈希校验通过。实机启动确认六关核心自检、两轮飞行页生命周期自检和既有硬件初始化全部通过；飞行页创建时 LVGL 仍为空闲 92964 B、最大连续块 92916 B，未因贴图恢复原生显示尺寸增加对象或堆占用。
- 2026-07-17 根据实机观感继续增大敌机显示：红色激光精英由 `48x48` 调为约 `56x56`，自定义精英由 `72x72` 调为约 `84x84`，Boss 由 `192x112` 调为 `240x140`。本次只改变 LVGL 图片缩放与对象边界，不改变贴图资源、敌人血量、碰撞半径、16 ms 刷新周期或固定对象池数量。
- 最终尺寸版本已通过 Linux 核心自检和 ESP-IDF 主工程完整构建，并烧录到 `/dev/ttyACM0`；应用镜像仍为 `0x193600`，分区剩余 `0x5CA00`（19%），bootloader、应用和分区表写后哈希校验通过。

## 关卡解锁奖励与进度限制

- 2026-07-17 按 `解锁奖励.txt` 将 `campaign_node_t` 的旧单一解锁说明拆成 `gate_reward` 和 `ship_reward`。关卡树右侧摘要和关卡详情分别使用青色显示门电路奖励、粉色显示飞船强化；没有对应奖励时不保留空行，六个特别关卡两项均为空，不再显示“解锁下一关”类文字。关卡树固定提示初始已拥有输入、输出门电路。
- 门电路奖励为：101 与非门，103 非门，201 与门，202 或门，203 或非门，301 异或门，302 同或门；102 及其余普通关卡不显示门电路奖励。飞船奖励为：101/102/103 飞船核心，201/401 推进速度，202/501/502 子弹伤害，203 星光护盾，301/402 发射速度，302/503/601 平行弹道，403 贯星能量炮，504/602 飞船核心。该表与六个特别关卡的固定成长配置一致。
- 关卡树正式启用前置限制，本次上电初始只有 101 可进入。节点在所有直接前置关卡都已完成后开放；锁定节点在地图中灰化，允许查看剧情和奖励，但详情状态显示“未解锁”且隐藏开始按钮，代码入口还会再次检查锁定状态。分支汇合 203 必须同时完成 201、202；最终特别关卡 650 必须同时完成 602 和第五个特别关卡 550。普通关卡判题成功和特别关卡胜利都会立即更新本次上电进度，不写 Flash。
- 设置页两个项目改为“扬声器测试”和“解锁所有关卡”。摇杆上下选择；选中扬声器测试后按住 Key0 右键持续播放 1 kHz，松开、切换项目或离开设置页立即静音；选中解锁所有关卡后按一次 Key0，会为本次上电设置全关开放覆盖，但不会伪造任何已完成标记。触摸设置卡片只改变选择，仍需右键执行。原硬件调试 screen 保留为内部诊断页面，但不再由设置菜单进入。
- 新增启动进度自检，验证初始仅 101 开放、所有前置 ID 存在、全关覆盖对全部节点有效、101→102 顺序、103→150、201/202 双汇合和 602+550→650 双前置。实机串口已输出 `campaign progression self-test passed`；六关飞行核心和两轮动态飞行页生命周期自检同时通过。
- `app_ui_font_cn_16.c` 只补充奖励与设置文案缺少的 11 个字形，`app_ui_font_cn_24.c` 只补充两个设置标题缺少的 8 个字形，未导入完整中文字库。最终使用 ESP-IDF 5.5.3 构建并烧录到 `/dev/ttyACM0`（ESP32-P4 v1.3），应用镜像 `0x194E60`，`0x1F0000` 分区剩余 `0x5B1A0`（18%），bootloader、应用和分区表写后哈希校验通过。启动后 23 节点、7 个常驻 screen 的 LVGL 空闲 116548 B、最大连续块 115768 B、碎片率 1%，串口观察约 20 秒无异常复位。

## 底板 I2C OLED 刷新优化

- 2026-07-18 保持底板链路 `100 kHz` 不变：该总线与 GT911 触摸共享，且经过 12 槽长链路，优先保证可靠性。一次 SSD1315 全帧写入约 100 ms，多块 OLED 仍由独立队列串行发送。
- 新槽位身份第一次读取后，不再等待下一轮 12 槽扫描才确认；在同一槽位约 `8 ms` 后立即做第二次 EEPROM ID 读取。槽位扫描周期仍为 `30 ms`，身份仍必须连续两次一致才确认，避免插拔瞬态误判。
- 底板 OLED 任务优先级调整为 `6`，高于身份扫描任务 `5`、低于红外任务；扫描、触摸和 OLED 写入仍通过原有 LVGL 适配器锁与底板互斥锁串行访问，避免共享 I2C 总线并发操作。
- OLED 写入失败时，任务在同一锁内将 TCA9548A 的 `GPIO47` 复位脚拉低约 `200 us`，调用 `i2c_master_bus_reset()` 复位共享控制器，再释放复位、等待 `1 ms` 并关闭两个 TCA 的全部通道；随后强制重新初始化 SSD1315 并立即重试一次。仍失败时按 `200/400/800 ms` 退避重新排队，避免错误状态长期阻塞扫描。
- 成功渲染会清零失败计数和重试截止时间；身份变化、首次显示或上次失败才会重绘，已正常显示的 OLED 不在每轮扫描中重复写整帧。
- 使用 ESP-IDF 5.5.3 完整构建并烧录 `/dev/ttyACM0`，ESP32-P4 v1.3 启动日志确认映射、17 关规则、飞行核心、LCD/触摸、音频和 I2C 初始化全部通过；应用镜像 `0x195280`，`0x1F0000` 分区剩余 `0x5AD80`（约 18%）。烧录后串口观察约 20 秒无 panic、看门狗或异常重启；底板积木插入后的实际 OLED 响应和自动恢复仍需在游玩界面用实物触发验证。
- 2026-07-18 修复快速拔插竞态：已确认槽位第一次 EEPROM 读取失败时，立即清除该槽位的 `has_rendered`、`oled_initialized` 和重试等待状态，但继续保留连续 3 次失败才移除的身份防抖。若积木在移除确认完成前重新插回，即使 ID 没有变化，也会重新排队 OLED、重新初始化 SSD1315 并显示门图；稳定读取成功后不会重复刷新。补丁已构建并烧录，应用镜像大小保持 `0x195280`，启动自检和串口运行正常，快速拔插场景仍需实机触发确认。

## 16 槽位 / 64 端口容量与映射迁移

- 2026-07-18 根据最新 `积木映射表` 将主工程统一迁移为 16 个槽位、64 个逻辑端口和四行四列底板布局。软件槽位按底板级联顺序排列：模块 1-4 使用 `0x70` 的 CH0-CH7，模块 5-8 使用 `0x71` 的 CH0-CH7；每个模块的上下两个槽位占用相邻通道。
- 新映射位置为：第一、二行从左到右依次为模块 4、3、2、1；第三、四行从左到右依次为模块 5、6、7、8。所有模块方向一致，上方槽位端口方向仍为上=3、下=1、左=2、右=0，下方槽位仍为上=5、下=7、左=4、右=6。
- `board_mapping`、`baseboard_ir`、`board_snapshot` 和 WS2812 使用统一容量宏，红外矩阵和 595/165 回读扩展为 64 位，WS2812 帧扩展为 64 颗，快照连接容量扩展为 64 条，连接调色板扩展为 32 色。64 位接收映射改为逆序还原 8 组 8-bit 数据，并增加 64 个 raw bit 的唯一性校验。
- 底板 I2C 槽位队列和 OLED 状态数组自动扩展到 16 个槽位；I2C 仍保持 100 kHz 总线和 30 ms 槽位周期。红外初始迁移时暂用 4167 us 槽位周期，随后已在下一节优化到接近 5 Hz。
- 使用 ESP-IDF 5.5.3 完整构建并烧录，应用镜像 `0x195440`，`0x1F0000` 应用分区剩余 `0x5ABC0`（约 18%），三段镜像写后哈希校验通过。启动串口已确认 4x4 网格、16 条 TCA 路由、64 位 IR 映射唯一性、16 节点线路布局和 64 颗 WS2812 帧初始化通过；首尾物理端口和 16 个模块的实际连线仍需在游玩界面逐项验证。

## 64 端口红外扫描时序优化

- 2026-07-18 将红外槽位周期从 `4167 us` 调整为 `3200 us`，发射稳定等待从 `3000 us` 调整为 `2400 us`，继续保留 3 次完整 `8+64` 位采样和逐位多数表决。64 个端口的名义扫描时间为 `204.8 ms`，加上整轮关闭发射和任务让步后目标约为 4.8-4.9 Hz。
- 相邻端口之间不再额外发送一帧 64 位全零数据；下一端口锁存前会先拉高 595 输出使能，在输出关闭期间移入下一份单热点掩码，因此不会在移位过程中同时点亮多个发射管。每轮最后一个端口完成后仍明确移入全零并关闭全部发射，退出游玩模式也保持相同安全关断。
- 扫描日志新增实际 `period`、`hz` 和累计 `overruns`，每 10 轮同时输出 `self_rx`、`cross_rx` 和连接数，用于后续实机判断时序余量和外壳光学串扰。输入任务在游玩模式下约每 5 个端口更新一次，名义输入刷新率随之提高到约 62.5 Hz；非游玩页面仍按约 20 ms 更新。
- 同时修正源码中“反转六组 8-bit”的过时注释，当前实际为逆序还原八组 8-bit 接收数据，不改变映射算法。ESP-IDF 5.5.3 构建并烧录成功，应用镜像 `0x1954D0`，应用分区剩余 `0x5AB30`（约 18%），三段镜像哈希校验和启动自检均通过。设备在主页连续运行约 50 秒无异常；实际扫描频率和 `overruns` 需进入普通关卡后由串口日志确认。

## 游玩页开关试玩与实时信号灯效

- 2026-07-18 游玩页右侧新增“开启开关试玩”操作项，摇杆上下在“启动飞船检查”和该操作之间选择，中键确认；再次确认或触摸“关闭开关试玩”退出。离开游玩流程时自动关闭并清空试玩状态，红外扫描、底板/本地 I2C、积木 OLED、LCD 实时拓扑和原真值表判题流程均保持独立运行。
- 开关试玩固定使用槽位7-10：SW4、SW3、SW2、SW1依次控制槽位7、8、9、10中的合法输入积木；目标槽位为空、ID无效或不是输入积木时忽略。固定槽位外的合法输入不再参与试玩传播，并通过游玩页状态提示玩家移动积木。
- 抽出统一组合逻辑传播接口，判题继续按关卡真值表独立枚举输入，开关试玩则使用固定槽位和当前四个钮子开关值传播。已知 `1` 使用绿色、已知 `0` 使用红色；输入和输出积木的四颗端口灯显示自身值，中间门只点亮参与有效连接的端口。有效连线两端同步显示其源端信号；信号尚不可求、积木身份无效、未定义端口或连接两端没有都落在有效积木端口上的线路保持熄灭，方向错误和单端多连仍使用原错误色。
- 开关稳定值变化、试玩开关切换和固定槽位输入变化均通过代数触发 WS2812 任务刷新，最长不依赖下一次完整红外轮询，任务仍以 50 ms 超时检查状态；普通模式下继续按每次完整红外扫描使用原多色连接灯效。
- 启动自检覆盖显式输入顺序传播、有效连线信号、目标槽位非输入、槽位空缺、SW3/SW4间隔绑定、固定槽位移除和错误位置清除。使用 ESP-IDF 5.5.3 完整构建并烧录到 `/dev/ttyACM0`，ESP32-P4 v1.3 串口确认固定槽位自检和全部既有自检通过；应用镜像 `0x196E30`，`0x1F0000` 分区剩余 `0x591D0`（18%），三段镜像写后哈希校验通过，启动后观察无 panic、看门狗或异常重启。新增提示文字均已包含在现有 16 px 中文子集内。

## 开关试玩下游灯效稳定性优化

- 2026-07-18 确认开关输入积木灯能够快速响应，而中间门、输出和连线偶发延迟或保留旧颜色。输入灯是直接使用已求值输入槽位覆盖，门和输出则依赖当前完整红外连接快照；一次红外漏检会使下游门暂时变为未知。另发现 WS2812 连续 RMT 事务之间没有明确的复位锁存低电平间隔，开关通知与整轮红外通知相邻时可能导致后一帧未被灯串锁存。
- 调试渲染现在使用独立的稳定连接快照：新发现的连接立即采用；连接首次漏检时保留，只有下一次完整红外轮询仍然漏检才移除。50 ms 兜底轮询不会重复累加漏检次数，I2C 槽位变化仍可立即更新门身份和端口角色。普通模式继续直接使用原始快照，不改变原有 LCD/判题数据源。
- 开关稳定值变化直接通知 WS2812 任务；灯效任务只在通知或状态代数变化时重算。每次发送后强制保持约 `80 us` 低电平作为 WS2812 reset/latch 间隔，并比较完整 64 灯帧，完全相同的帧不重复发送。新增调试日志输出状态代数、开关掩码、已知门掩码、值掩码和稳定连接数，便于区分逻辑传播问题与灯串接收问题。
- 稳定快照自检覆盖：首次连接、完整红外轮询第一次漏检仍保留、同一轮重复检查不计数、第二轮漏检才移除。WS2812 任务栈由 `4096 B` 增至 `6144 B`，容纳 16 槽快照复制和调试渲染的临时数据。
- 使用 ESP-IDF 5.5.3 完整构建并烧录到 `/dev/ttyACM0`，ESP32-P4 v1.3 启动确认稳定快照自检、17 关逻辑、映射、布局和飞行页自检全部通过；应用镜像 `0x196D00`，`0x1F0000` 分区剩余 `0x59300`（18%），三段镜像哈希校验通过。启动后观察约 11 秒无 panic、看门狗或异常重启；实际多级电路灯效仍需在游玩页用实体开关和积木验证。

## I/O 四端口显示与旧位置残留修复

- 2026-07-18 修复开关试玩中 INPUT/OUTPUT 周围四颗 WS2812 偶发只亮 1 颗或 3 颗的问题。调试模式下 I/O 积木四颗灯现在优先按该积木的已知信号统一显示，不再被其余物理侧产生的 `UNKNOWN_ENDPOINT` 或 `UNUSED_PORT` 悬空检测压灭；中间逻辑门仍保持悬空线路熄灭策略。
- 针对积木移动后原槽位灯保留旧红/绿的问题，调试模式增加约 `250 ms` 的完整帧心跳重发。即使当前帧字节内容未变化，也会重新发送并经过 `80 us` reset/latch 间隔，覆盖偶发 WS2812 灯串未锁存或单次传输丢失的情况；积木位置变化时仍立即发送新帧。
- 使用 ESP-IDF 5.5.3 完整构建并烧录 `/dev/ttyACM0`，应用镜像 `0x196D80`，`0x1F0000` 分区剩余 `0x59280`（18%），bootloader、分区表和应用写后哈希校验通过。ESP32-P4 v1.3 启动确认既有全部自检通过，串口观察约 15 秒无 panic、看门狗或异常重启；四灯完整显示和积木移动后的实体灯串效果需继续在游玩页验证。

## 上电尾端 WS2812 残留清除

- 2026-07-18 排查到上电时槽位16下、左、右端口灯可能保留绿色：WS2812 任务首次进入非游玩分支时原本认为没有活动输出，因此不会再发送第二次全黑帧；最后几颗灯可能仍处于电源上电默认态或上一次复位残留态。16槽映射自身校验无重复，槽位16下/左/右为逻辑端口 `63/60/62`。
- WS2812 连接任务现在启动时强制发送一次全黑帧，并继续在退出游玩时清零；保留帧后的 `80 us` 锁存低电平。该修改不改变端口映射或游玩灯效逻辑。

## 64 灯独立跑马灯硬件排查

- 2026-07-18 将 `test_backups/ws2812_48led` 独立跑马灯备份临时扩展为 64 灯版本，仍使用 GPIO46、LDO4 3.3V 和原始红色单灯跑马逻辑，目录名保留以兼容已有备份路径。该固件不启动红外、I2C、LCD 或主项目逻辑，可单独验证新增最后16颗 WS2812。

## 游玩页判题成功状态回收

- 2026-07-18 修复普通关卡判题成功后返回游玩页，再选择“开启开关试玩”并按中键会重新进入成功页的问题。成功页返回游玩页时立即将判题器恢复为空闲状态；成功页跳转只响应当前关卡本次刷新中新产生的 `GAME_JUDGE_PASSED`，开关试玩、积木编程器或拓扑刷新不会重复消费旧的通过状态。开关试玩与“启动飞船检查”的中键分支保持独立。

## 开关试玩固定槽位绑定

- 2026-07-19 将开关试玩的输入绑定从“插入顺序队列”改为固定物理槽位：SW4→槽位7、SW3→槽位8、SW2→槽位9、SW1→槽位10。槽位7-10对应当前四行布局面向用户的最左列，从上到下排列；目标槽位为空、ID无效或插入的不是输入积木时，对应开关忽略，不影响其他门电路。
- 传播时先按槽位顺序收集有效输入，再把反向 SW 位重新压缩到传播输入位，因此即使槽位7/8为空，槽位9仍由SW2控制，不会错误变成SW1。固定槽位外检测到合法输入积木时，开关试玩状态区显示红色提示“输入积木请放到最左侧一列”；该输入不分配开关，依赖它的信号保持未知并熄灭。普通真值表判题不受此调试绑定规则影响。
- 启动自检新增目标槽位非输入、槽位空缺、SW3/SW4间隔绑定、固定槽位移除和错误位置清除覆盖。

## 输入积木四端口分线与门电路编程顺序

- 2026-07-19 输入积木四个物理端口统一视为等价输出端口；同一个输入积木可以同时用不同端口连接到多个下游门输入。底板快照、LCD线路、WS2812信号传播和判题预检查均保留这些独立有效连线，单个端口自身仍不能重复连接多个端点。
- SSD1315 非门图标改为单条居中的输入线，其他双输入门仍保留上下两条输入线。
- 本地积木 EEPROM 编程器的左右切换顺序固定为：输入、输出、与非门、非门、与门、或门、或非门、异或门、同或门。输入和输出始终可选；与非门、非门、与门、或门、或非门、异或门、同或门分别只有在通过 101、103、201、202、203、301、302 关并获得对应门奖励后才加入可选列表。未解锁门不会在左右切换或当前候选 OLED 中显示；“解锁所有关卡”设置不伪造这些门奖励。
- 启动自检新增输入四端口两路分线、NAND 双输入求值、门编程顺序和初始未解锁门过滤检查。
- 2026-07-19 修正解锁进度自检：201、202 完成后仅开放 203，完成 203 后才解锁或非门；与实际奖励规则一致。该修正已使用 ESP-IDF 5.5.3 构建并烧录到 `/dev/ttyACM0`，启动串口确认 16 槽位映射、17 条规则、输入分线、编程器顺序、关卡解锁和 LVGL 页面自检均通过，未出现重启。

## UI 与打飞机音效

- 2026-07-19 在现有 ES8311/I2S 扬声器任务上增加非阻塞效果音请求队列。选择、确认、返回、错误、发射、击破敌机、玩家受击、护盾/技能、胜利和失败分别使用短促的升降调音效；本地积木 ID 切换、LCD 页面交互、普通关卡判题结果和打飞机游戏事件均已接入。
- 音效由 16 kHz 音频任务实时生成，单次请求只保留最新效果，避免连续按键或游戏帧堆积延迟；按住设置/诊断页的右键播放 1 kHz 测试音时，会清空待播放效果并优先保持测试音。效果音不新增音频外设或阻塞 LVGL。
- 使用 ESP-IDF 5.5.3 构建并烧录到 `/dev/ttyACM0`，应用镜像 `0x197D10`，`0x1F0000` 分区剩余 `0x582F0`（约 18%）。串口确认 ESP32-P4 v1.3、全部既有启动自检、ES8311 双工音频和音效任务正常启动，未出现 panic、看门狗或异常重启；各音效的最终音量与听感由实机交互继续确认。

## 中文字库完整性修复

- 2026-07-19 对所有 LCD 实际文案按字体绑定重新审计。主 UI 16 px 补入“列”，修复“输入积木请放到最左侧一列”的缺字；飞行游戏 16 px 补入奖励关详情、结算页和全角标点缺失的 `列、卫、吸、守、挡、修、再、别、功、整、架、特、船、试、需、！、【、】、了、事、件、住、入、前、危、变、古、后、在、它、实、开、恢、打、收、文、明、正、理、罩、让、过、遇、遭、险、高`。
- 新增 `tools/check_ui_font_glyphs.py`，按普通页面、标准关卡、奖励关和飞行游戏的实际字体分配检查缺失字形；当前审计结果为 app UI 16 px 500 个、app UI 24 px 87 个、shooter 16 px 277 个必需非 ASCII 字符全部覆盖。
- 使用 ESP-IDF 5.5.3 重新构建并烧录 `/dev/ttyACM0`，应用镜像 `0x1993C0`，`0x1F0000` 分区剩余 `0x56C40`（约 17%）。启动串口确认映射、规则、LVGL、音频和既有自检全部通过，未出现异常重启。

## 云端语音功能移植前版本备份

- 2026-07-19 创建 `version_backups/2026-07-19_pre_cloud_voice/`，保存当前主工程源码、16槽位/64端口配置、UI及字库、打飞机核心、当前应用/bootloader/分区表镜像、硬件资料和开发文档，作为接入联网语音大模型前的回退基线。
- 备份未重复复制旧测试备份、`managed_components/` 和 build 缓存；依赖锁定文件、SDK 配置和可烧录镜像已保留。

## 摇杆右向 GPIO2 与 16 MB Flash 分区

- 2026-07-19 将摇杆右方向从 GPIO54 改为 GPIO2；启动日志确认逻辑映射为 `UP=36 DOWN=48 LEFT=23 RIGHT=2`。GPIO54 不再由主项目摇杆占用。
- 按开发板实际 16 MB Flash 更新配置，`sdkconfig`/`sdkconfig.defaults` 启用 16 MB Flash，`partitions.csv` 将 `factory` 分区扩展为 `0xf00000`（15 MiB），保留 `nvs` 和 `phy_init` 分区。
- 使用 ESP-IDF 5.5.3 完整构建成功，应用镜像 `0x1993d0`，应用分区剩余 `0xd66c30`（约 89%）；分区大小检查通过。
- 已烧录 `/dev/ttyACM0`，bootloader、分区表和应用镜像哈希校验全部通过。启动串口确认 Flash Size 为 16 MB、16 槽位/64 端口映射、红外、WS2812、LCD、I2C、音频及既有自检均正常，未出现 panic、看门狗或异常重启。

## ESP32-C6 联网测试

- 2026-07-19 在 ESP32-P4 Function-EV-Board 上加入一次性 ESP-Hosted 联网测试任务，使用板载 ESP32-C6 作为 Wi-Fi 协处理器。P4 与 C6 通过 4-bit SDIO 连接：CLK=`GPIO18`、CMD=`GPIO19`、D0-D3=`GPIO14/15/16/17`、C6 复位=`GPIO54`，速率 `40 MHz`。Wi-Fi 依赖 `esp_hosted`、`esp_wifi_remote`，测试入口为 `main/c6_network_test.c`。
- 测试配置为扫描附近热点后连接手机热点 SSID `Redmi K70`；密码已写入本地 `sdkconfig`/`sdkconfig.defaults`，不在日志中重复明文记录。任务完成扫描、STA 连接、DHCP 获取地址和 DNS 查询后自动退出，不改变主项目后续硬件任务。
- 首次启动曾在 `sdio_mempool_create()` 处因内部 DMA 内存不足触发断言。启用 `CONFIG_ESP_HOSTED_MEMPOOL_PREFER_SPIRAM=y` 后，SDIO 缓冲池改用 PSRAM，启动恢复正常。
- 串口实测：SDIO 从设备识别为 `esp32c6`，初始化成功；扫描到 `19` 个热点；成功连接 `Redmi K70`，获取 IP `10.171.108.96`；`example.com` DNS 查询成功并解析到 `104.20.23.154`。说明 P4-C6 SDIO、C6 Wi-Fi、热点认证、DHCP 和 DNS 链路均已跑通。
- 启动日志仍提示 `Version mismatch: Host [2.12.0] > Co-proc [0.0.0]`。当前联网功能实测可用，但板载 C6 的 ESP-Hosted slave 固件版本偏旧；后续若出现 RPC 超时或连接不稳定，应优先更新 C6 slave 固件，再重新验证。

## 火山引擎实时语音接入基础

- 2026-07-19 按队友 ESP32-S3 项目的实现方式加入火山引擎实时对话 WebSocket 客户端，默认地址为 `wss://openspeech.bytedance.com/api/v3/realtime/dialogue`，模型为 `O`，TTS 音色为 `zh_female_vv_jupiter_bigtts`。鉴权字段使用 `X-Api-App-ID`、`X-Api-Access-Key`、`X-Api-Resource-Id=volc.speech.dialog`、`X-Api-App-Key` 和随机 `X-Api-Request-Id`。
- 新增 `main/volcengine_voice.c/.h`，协议流程为 StartConnection、StartSession、16 kHz 单声道 PCM 持续上行、EndASR、TTS PCM 分片下行和 FinishSession。WebSocket 分片重组、TLS 证书束、会话超时、断线处理、少于 10 帧的短录音取消和 6 秒最长录音均已实现。
- 语音任务只在游玩页面且未插入本地编程积木时响应 Key0；按住开始采集，松开提交。当前页面状态包含“正在听你说”“正在寻找提示”“提示来了”“未连接网络”等固定文案；正常关卡离线可继续运行，离线时长按右键只提示网络不可用。
- `audio_self_test` 增加两个 PSRAM 环形缓冲：256 KiB 麦克风采集缓冲和 256 KiB TTS 播放缓冲。麦克风和扬声器仍分别由原有音频任务独占访问，效果音在云端语音会话期间自动让位，避免 ES8311 并发读写。
- StartSession 当前严格使用队友项目已验证的标准字段：`dialog.extra.model/input_mod`、16 kHz 单声道 PCM 的 `asr.audio_info`、`end_smooth_window_ms`、`tts` 配置。此前试加的 `system_prompt`、`circuit_context` 已移除，因为实时对话资源不接受这两个自定义字段；儿童方向性提示和电路摘要后续需通过该接口支持的消息方式注入。
- 三项火山引擎凭据已写入本地 `sdkconfig`，没有写入源码、`sdkconfig.defaults` 或日志；日志和版本库均不重复记录密钥。无网络时仍保持离线兼容。
- 首次联网启动发现 ESP-IDF TCP/WebSocket 传输层 HTTP 升级缓冲区默认仅 `1024` 字节，火山引擎握手响应超过该值而报 `Header size exceeded buffer size`。已在 `sdkconfig` 与 `sdkconfig.defaults` 将 `CONFIG_WS_BUFFER_SIZE` 调整为 `4096`，应用层数据缓冲仍为 `4096` 字节。
- 热点恢复后实测 P4-C6 已完成扫描、连接、DHCP、DNS 和 TLS 证书验证；火山引擎握手返回 HTTP `403`，响应正文为 `requested resource not granted`，说明当前凭据没有被授予 `volc.speech.dialog` 资源。已增加 401/403 专用日志和界面提示“云端服务未授权”，不再误报为普通断线；需要在服务控制台开通该资源或换用已授权凭据后再做协议会话验证。
- 针对上述错误提示使用 ESP-IDF 5.5.3 重新完整构建并烧录 `/dev/ttyACM0`；应用镜像 `0x2502e0`，`0xf00000` 应用分区剩余约 85%，bootloader、应用和分区表哈希校验通过。启动串口确认 P4 v1.3、16 槽位/64 端口映射、红外、WS2812、LCD、I2C、音频及既有自检均正常。
- 2026-07-19 按要求轮换本地 `sdkconfig` 中的三项云端凭据（密钥值不写入源码或日志）。使用新凭据实测已通过 `wss://openspeech.bytedance.com/api/v3/realtime/dialogue` 的 TLS、WebSocket 和 `StartConnection` 协议握手；旧凭据的资源未授权问题不再出现。错误协议帧现在会记录服务端错误正文，便于后续定位 StartSession 或音频参数问题。
- 2026-07-19 修复按键后“云端暂时没有回应”：删除实时对话接口不支持的自定义会话字段，恢复标准 StartSession 请求；使用 ESP-IDF 5.5.3 构建并烧录，应用镜像 `0x2500a0`，分区剩余约 85%。启动串口确认 WebSocket 和 `StartConnection` 正常，实际按键录音回复需在设备游玩页按住右键进一步验证。
- 2026-07-19 按要求将本地 `sdkconfig` 切换到服务端此前返回的同应用 App Key 配置；密钥值不写入日志。重新构建并烧录 `/dev/ttyACM0` 成功，应用镜像 `0x250090`，`0xf00000` 应用分区剩余约 85%。本次启动串口确认 C6 获取 IP、DNS、TLS、WebSocket 和 `StartConnection` 均成功，未观察到启动异常；仍需在游玩页实际按住右键触发 `StartSession`，确认服务端鉴权及音频会话是否完整通过。
- 2026-07-19 实机连续语音复现：三次均收到 `Volcengine session started`，等待云端回复约 9-11 秒后出现 `esp-aes: Failed to allocate memory`，紧接着 `esp_tls_mbedtls: read error :-0x0001`、WebSocket 接收失败并自动重连；未出现 panic、看门狗或应用重启。失败点发生在 TLS 数据读取阶段，说明 AES-DMA 所需的内部/DMA-capable 内存申请失败，不能归因于 Wi-Fi、TLS 握手或 App Key 鉴权。现有 `i2c.master: probe device timeout` 会并行出现，但 UI 健康检查仍正常，暂判定为独立的 I2C 探测告警。后续应增加内部堆余量/最大连续块监控，并评估 mbedTLS 内存分配策略、TLS 缓冲区和 WebSocket 重连对象回收。
- 2026-07-19 增加语音阶段内存观测：记录 WebSocket 连接、StartSession 发送、SessionStarted、协议错误和断开时的内部 RAM、DMA-capable RAM、PSRAM 可用量/最大连续块/历史最低值。实测会话建立时 DMA 可用约 14-15 KiB、最大连续块约 9 KiB，AES-DMA 失败前降至约 4-6 KiB、最大连续块约 1 KiB；断开后恢复至约 20 KiB，确认是 TLS AES-DMA 瞬时内部内存不足或碎片化。
- 2026-07-19 做软件 AES A/B 测试：临时关闭 mbedTLS 硬件 AES/GCM 后重新构建烧录；首次会话尚未进入 TLS 内存错误，就在现有 I2C 探测超时期间触发 `Store access fault`，回溯位于 `i2c_isr_receive_handler -> i2c_ll_read_rxfifo`，设备自动重启。因此软件 AES 方案暂不采用，已恢复硬件 AES 配置并重新构建烧录；恢复版启动、C6 联网、TLS 和 WebSocket 握手正常，内存观测代码保留。
- 2026-07-19 硬件 AES 恢复版再次连续复测：两次均正常收到 `StartSession`/`Volcengine session started`，建立时 DMA 可用约 `6.6 KiB`、最大连续块约 `3 KiB`；约 6-8 秒后再次出现 `esp-aes: Failed to allocate memory` 和 `ESP_ERR_MBEDTLS_SSL_READ_FAILED`，随后 WebSocket 重连。断开事件后 DMA 可用恢复约 `22.9 KiB`，无服务器错误正文、panic 或重启。与前一轮一致，问题稳定复现于本地 TLS AES-DMA 读取阶段。
- 2026-07-19 此前测试启用 mbedTLS 外部内存分配并保留固定 TLS 缓冲：`CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC=y`，固定输入/输出缓冲分别为 8192/4096 字节，动态缓冲保持关闭。该版本完整构建通过，应用镜像约 `0x250270`，应用分区剩余约 85%；后续实测发现 8192 字节输入缓冲不足，已在后续版本调整为 16384 字节。
- 2026-07-19 此前版本开启服务器关闭后的自动重连，并曾设置 15 秒心跳、30 秒 PONG 超时；串口实测该服务端不稳定返回 Pong，导致客户端误断线，后续版本已关闭 Pong 超时主动断线。服务器关闭重连、正常/异常关闭区分、错误原始文本记录和客户端启动失败句柄释放均保留。
- 2026-07-19 根据串口分析进一步调整：固定 TLS 输入缓冲改为 16384 字节，输出缓冲保持 4096 字节；关闭“Pong 超时主动断线”，保留 30 秒低频 Ping 和服务器关闭自动重连。新增 `WEBSOCKET_EVENT_CLOSED` 与异常 `WEBSOCKET_EVENT_DISCONNECTED` 的独立状态清理，断开时释放未完成的 WebSocket 分片缓冲；错误日志改为记录 WebSocket 原始传输文本，避免使用组件尚未填充的错误结构字段。
- 2026-07-19 实机验证本版两次连续语音会话：均完成 `StartSession`、`SessionStarted`、服务端回复和 `SessionFinished`，未再出现 `-0x7100`、`esp-aes: Failed to allocate memory`、Pong 超时、panic 或重启。会话期间红外全扫描保持约 4.54～4.59 Hz；I2C 探测 timeout 仍会偶发打印，但未影响语音会话，暂按独立告警处理。应用镜像约 `0x250570`，应用分区剩余约 85%。
- 2026-07-19 按官方 Realtime API 文档尝试注入语音指导上下文：O 模型支持的标准字段为 `dialog.bot_name`、`dialog.system_role`、`dialog.speaking_style`，不再使用此前不受支持的自定义 `system_prompt`/`circuit_context`。每次 StartSession 动态加入当前关卡目标、已识别积木、有效端口连接和无效连接数量；固定人设要求使用中文、面向儿童、只给方向性提示、避免 API/I2C/红外等开发术语。编译并烧录成功，应用镜像约 `0x250eb0`，分区剩余约 85%；本次串口窗口未捕获按键会话，模型实际语气仍需设备上按住右键说话验证。
- 2026-07-19 将扬声器软件输出音量从 `70` 调整为 `100`，通过 `esp_codec_dev_set_out_vol()` 应用。使用 ESP-IDF 5.5.3 编译并烧录 `/dev/ttyACM0` 成功，应用镜像 `0x250eb0`，应用分区剩余约 85%；启动串口确认 ESP32-P4 v1.3、音频、LCD、红外、WS2812、I2C 和 C6 联网初始化正常。高音量下可能出现失真或扬声器过载。

## 云端语音卡顿与连接稳定性优化

- 2026-07-19 针对语音断续和云端响应超时检查确认：WebSocket 组件原先收发共用一把锁，录音以 512 字节/16 ms 小包高频上传；TTS 收到首包后立即播放且没有抗抖预缓冲，音频任务优先级也低于 WebSocket 任务。这些条件叠加时，上行写入会阻塞下行接收，网络瞬时抖动会直接形成扬声器静音缺口。
- 启用 `CONFIG_ESP_WS_CLIENT_SEPARATE_TX_LOCK=y`，发送锁等待上限为 1000 ms，使音频上行和云端下行不再互相占用同一客户端锁。录音改为合并 4 个 512 字节 PCM 帧后，以 2048 字节/约 64 ms 为单位上传，音频包发送超时限制为 1 秒；会话启动超时从 12 秒放宽到 20 秒，并从 StartSession 实际发送成功后开始计时，松键提交后的云端回复超时为 30 秒。
- TTS 播放增加 8192 字节预缓冲，对应 16 kHz 单声道 PCM16 的约 256 ms；首次播放和网络抖动后的恢复都先达到该水位，服务端接收回调改为非阻塞写入 256 KiB PSRAM 播放环形缓冲，避免 WebSocket 接收任务因等待扬声器而停顿。扬声器任务优先级调整为 6，麦克风任务调整为 5，音量保持 100。
- C6 Wi-Fi 关闭省电模式以降低实时语音延迟，自动重试次数由 3 次增加到 8 次，并记录 802.11 断线原因。串口实测热点 RSSI 约 -32 至 -37 dBm，启动时前两次连接分别出现 reason 202（认证失败）和 reason 205（连接失败），第三次成功取得 IP；随后 DNS、TLS、WebSocket 和火山引擎协议握手均成功，协议就绪后的观察窗口内没有再次断线、TLS 错误、panic 或重启。板载 C6 仍报告 Host 2.12.0 与协处理器 0.0.0 版本不匹配，这是后续若仍出现 ESP-Hosted RPC 异常时需要优先处理的硬件固件风险。
- 使用 ESP-IDF 5.5.3 完整编译并烧录 `/dev/ttyACM0` 成功，应用镜像 `0x251810`，应用分区剩余约 85%，全部镜像哈希校验通过。本次串口窗口未捕获用户实际按键语音会话，因此录音、ASR、TTS 连续性的最终效果仍需在游玩页实际长按右键验证。
- 上述版本实机发现联网后在游玩页按下右键会立即重启。原因是音频上传包由 512 字节扩大到 2048 字节后仍作为 `voice_task` 局部数组存在，而 StartSession 同一调用链还同时在栈上创建 3072 字节系统提示、768 字节语气提示和约 1 KiB 电路快照，超过 `voice_cloud` 的 8 KiB 任务栈安全余量。
- 修复时没有继续扩大内部 RAM 栈，而是将 2048 字节上传缓冲改为语音任务启动时一次性申请的 PSRAM 缓冲；提示词和电路快照合并为一次 StartSession 使用的 PSRAM 工作区，用完立即释放。编译后的 RISC-V 函数栈帧检查确认 `voice_task` 为 128 字节、`send_start_session` 为 80 字节，大数组已不再占用该任务调用栈。
- 修复版使用 ESP-IDF 5.5.3 编译并烧录 `/dev/ttyACM0` 成功，应用镜像 `0x251850`，应用分区剩余约 85%，镜像哈希校验通过；不触发 USB 复位的串口方式连续观察约 70 秒，固件未自行崩溃或重启。观察窗口内没有捕获到实际右键事件，仍需实机再次按键确认完整会话。

## 音量分离与积木语义语音提示

- 2026-07-19 将普通 UI、判题和打飞机等游戏音效设为 `70`，云端 TTS 语音保持 `100`。ES8311 音量只由扬声器任务在普通音效与语音播放状态切换时调整，避免控制命令与 I2S 写入并发；开机默认使用音效音量，语音结束后自动恢复。
- 火山引擎 StartSession 上下文不再向模型提供“槽位几、上/下/左/右端口”的连线描述。当前快照按积木类型统计数量，并依据端口角色生成“输入积木的输出端连接到与非门积木的输入端”一类语义描述；存在多个同类积木时使用“第1块输入积木”等名称区分。提示词同时明确禁止模型向儿童复述槽位号和物理端口方向。
- 使用 ESP-IDF 5.5.3 增量构建通过，应用镜像约 `0x251ac0`，15 MiB 应用分区剩余约 85%。本次未烧录。

## 长时间游玩异常重启初步分析

- 本次没有捕获到对应异常重启的 panic 回溯，因此尚不能把根因唯一归结为某个模块。普通游玩页约 2.5 分钟的既有健康采样中，LVGL 空闲内存约 `115820` 字节、最大连续块约 `114940` 字节、碎片率约 `1%`、完整性检查为 `ok`，UI 任务栈余量约 `4.7 KiB`；射击游戏页面反复创建/删除自检后内存也能恢复。因此目前没有证据支持持续 LVGL 对象泄漏是首要原因。
- 风险最高的是底板 I2C 扫描与 OLED 恢复路径。历史实机已捕获过 `Store access fault`，回溯位于 `i2c_isr_receive_handler -> i2c_ll_read_rxfifo`；当前长时间游玩又会持续执行 16 槽位轮询、探测、OLED 重试和总线恢复，并偶发 `i2c.master: probe device timeout`。这证明 I2C 驱动路径曾真实崩溃，但没有本次回溯时仍不能确认每次蓝屏都由它引起。
- 第二风险是反复云端语音会话造成内部堆/DMA 堆碎片。语音提示工作区和 2048 字节录音上传缓冲已移至 PSRAM，但协议封包、JSON、WebSocket 下行分片仍包含多次动态分配；项目此前已实测 AES-DMA 在最大连续块过小时分配失败。该问题目前更常表现为 TLS 断线而非直接 panic，但长时间反复会话仍可能放大内存压力。
- ESP-Hosted 还持续报告 P4 Host `2.12.0` 与 C6 协处理器 `0.0.0` 版本不匹配，官方组件日志提示可能造成 RPC 超时。它能解释联网不稳定，但在缺少崩溃现场时不能单独解释蓝屏重启。当前固件已启用 FreeRTOS 栈溢出 canary，但未启用 Flash coredump、堆 poisoning 或编译器栈保护；下一步应先保存一次真实回溯和重启原因，再针对命中的 I2C、堆、栈或看门狗路径修复。

## 通关记录断电保存与设置初始化

- 2026-07-19 新增 `main/campaign_progress.c/.h`，使用 NVS 命名空间 `tuco_game` 按关卡编号保存已完成状态。每次普通关卡判题通过或奖励关通关后立即提交对应记录；启动时按关卡 ID 恢复，因此断电重启后已完成关卡、后续解锁关系和奖励门电路选择状态都会保留。未保存网络、云端密钥或其他系统配置。
- 设置页新增“初始化”功能。选中后按中键会擦除 `tuco_game` 命名空间、清空内存中的通关记录、关闭本次运行的“解锁所有关卡”辅助状态，并回到初始关卡；擦除失败时保留当前记录并显示错误提示。该操作不会调用全局 `nvs_flash_erase()`，不会擦除 Wi-Fi 或云端配置。
- 设置页现有“扬声器测试”和“解锁所有关卡”统一改为中键启用；扬声器测试在该项选中且中键按住期间播放 1 kHz，松开停止。设置项由摇杆上下在三个选项间循环选择，左键返回逻辑不变。
- 为新增设置文案及既有实际 UI 文案补齐主界面 16 px 字库缺失的 42 个字形。`tools/check_ui_font_glyphs.py` 当前结果：app UI 16 px、app UI 24 px、shooter 16 px 全部通过。
- 使用 ESP-IDF 5.5.3 构建通过，应用镜像 `0x2553c0`，15 MiB 应用分区剩余约 84%。本轮未烧录。
- 随后已将该版本完整烧录到 `/dev/ttyACM0`。bootloader、分区表和应用镜像均通过哈希校验；启动日志确认 16 MB Flash、16 槽位/64 端口映射、通关记录恢复、LVGL 页面自检、音频、I2C、红外、WS2812 和 C6 联网初始化正常。串口观察窗口内未出现 panic、看门狗或异常重启。

## 存档恢复导致的启动重启修复

- 2026-07-19 首次烧录带断电存档功能的版本后出现无限重启。串口回溯明确显示：`programmer_gate_selection_self_test()` 因固定假设“空白进度下输出门之后没有可选门”返回 `ESP_ERR_INVALID_STATE`，随后 `app_main` 的 `ESP_ERROR_CHECK(block_i2c_init())` 主动调用 `abort()`；不是 NVS 损坏、PSRAM 故障或硬件供电问题。

- 已将编程器自检改为依据当前真实存档验证：首个候选必须是输入积木，首个候选之后的候选必须是当前已解锁项目。实际门电路顺序和未解锁门过滤逻辑不变，已有通关记录不会再触发启动自检失败。
- 修复版已重新构建并烧录 `/dev/ttyACM0`，应用镜像仍为 `0x2553c0`，bootloader、分区表和应用哈希校验通过。启动串口确认 `campaign progress restored from flash`、编程器/baseboard I2C 初始化、`main_task: Returned from app_main()` 均正常；随后 C6 Wi-Fi、DNS 和云端 WebSocket 也完成初始化，观察窗口内没有再次重启。

## 主机端后台串口监视

- 2026-07-19 按要求保持原固件逻辑，不在 ESP32-P4 内增加断电日志、全局日志拦截或 Flash coredump。分区表恢复为 24 KiB NVS、4 KiB PHY 和从 `0x10000` 开始的 `0xFF0000` 单 factory 应用分区；关卡存档、Wi-Fi 和云端配置均未擦除。
- 回退版本使用 ESP-IDF 5.5.3 完整编译并烧录 `/dev/ttyACM0` 成功，应用镜像大小为 `0x2553C0`，应用分区剩余 85%，所有镜像写后哈希校验通过。实机启动完成游戏自检、LCD/LVGL、音频、I2C、ESP-Hosted、Wi-Fi 和云端协议初始化，未出现异常重启。
- 调试期间由电脑上的 ESP-IDF monitor 持续采集串口，记录文件为项目根目录 `后台串口监视.log`。监视器使用当前 `build/tuco_baseboard_firmware.elf` 实时解析异常地址；测试期间应保持 USB 连接，若设备崩溃或重启，通知后直接检查该文件末尾即可。

## ESP32-P4 I2C 接收状态补丁

- 2026-07-19 长时间游玩约 417 秒后捕获到完整异常：Core 0 在 `i2c_ll_read_rxfifo()` 中触发 `Store access fault`，接收目标为 `NULL`、长度为 1，硬件地址 `0x500C4000` 对应本地积木编程接口使用的 I2C0。崩溃前约 6 秒出现一次 `i2c.master: probe device timeout`；同期 LVGL 完整性、内存和 UI 任务栈均正常。
- 根因与乐鑫 `esp-idf#18787` 一致：ESP-IDF 5.5.3 新 I2C master 驱动在读事务因 NACK/超时异常结束时可能残留 `contains_read=true`，下一次 probe/写事务完成中断会被误当成接收事务，最终向空缓冲区写入。
- 项目新增 `components/esp_driver_i2c`，作为 ESP-IDF v5.5.3 同名组件的本地覆盖；未修改电脑上的全局 IDF。补丁在每个新事务初始化 `read_buf_pos` 后同步执行 `contains_read=false`，确保失败读事务的状态不会污染后续 probe 或写事务。`components/esp_driver_i2c/PATCH.md` 记录了补丁目的和后续移除条件。
- 完整重新配置确认构建系统使用项目内 `components/esp_driver_i2c/i2c_master.c`，随后编译和烧录 `/dev/ttyACM0` 成功。应用镜像为 `0x2553C0`，应用分区剩余 85%，bootloader、分区表和应用镜像写后哈希校验均通过。补丁版本启动完成主程序自检、LCD/LVGL、音频、I2C 和 ESP-Hosted 初始化，未出现启动异常。
- 补丁前的完整崩溃现场保存在 `后台串口监视_补丁前崩溃.log`；补丁版本继续写入新的 `后台串口监视.log`，需要通过长时间实际游玩验证同一 `i2c_ll_read_rxfifo(ptr=NULL)` 异常不再出现。

## 云端语音回答提示词精简

- 2026-07-19 更新 `main/volcengine_voice.c` 注入的 `system_role` 和 `speaking_style`：云端助手只回答玩家刚才的问题和当前电路，不再开场复述或罗列完整电路。
- 新增硬性回答约束：每次回复不超过 30 个汉字，通常只给一句方向性提示；保留使用积木名称描述连接的规则，并继续屏蔽槽位、端口方向和开发接口等术语。
- 使用 ESP-IDF 5.5.3 完整构建并烧录 `/dev/ttyACM0` 成功，应用镜像 `0x2554D0`，应用分区剩余约 85%，三个镜像写后哈希校验均通过。烧录后串口确认主程序、Wi-Fi、DNS 和云端 WebSocket 正常启动，观察约 33 秒未出现 panic 或异常重启。

## 云端结构化动作能力验证与连接自恢复

- 2026-07-20 根据长时间空闲后语音请求超时的串口时间线，为 `main/volcengine_voice.c` 增加 WebSocket 自恢复：协议连接空闲超过 10 分钟时，在下一次语音请求前主动重建连接；`StartSession` 发送失败、20 秒未收到 `SessionStarted` 或底层 WebSocket 错误时也强制销毁并重建客户端。重建只在语音任务中执行，避免从 WebSocket 事件回调中调用停止接口；右键仍按住时允许重连后继续启动本次录音。
- 使用与设备相同的火山实时对话配置进行了独立主机协议测试。普通中文问题完整收到最终 ASR、事件 350/352 TTS 和流式事件 550；事件 550 的固定结构只有 `content`、`question_id`、`reply_id`，模型文本位于连续的 `content` 分片中。
- 严格要求模型返回 `reply` 和 `action` JSON 后，`{\"reply\":...\"action\":...}` 仍被逐字放入事件 550 的 `content`，同时服务器开始发送事件 352 TTS 音频；没有发现独立于朗读文本的隐藏动作字段。当前实时对话接口因此不能直接承载既不朗读又可供固件执行的模型工具参数。
- 按开发约定在该能力验证处暂停：未创建本地 Agent、未加入动作解析、未修改 WS2812 端口提示灯逻辑。后续需要云端提供原生工具调用/独立结构化字段，或重新确认允许采用其他架构后才能继续。
- WebSocket 自恢复补丁已使用 ESP-IDF 5.5.3 完整构建并烧录 `/dev/ttyACM0`，应用镜像 `0x255890`，15 MiB 应用分区剩余约 85%，bootloader、分区表和应用镜像写后哈希校验均通过。启动串口确认 LCD/LVGL、音频、I2C、红外和 ESP-Hosted 正常初始化且没有 panic；当时扫描列表中没有配置的 `Redmi K70` 热点，因此板端未继续到 Wi-Fi 和云端握手，后台串口监视继续保留。

## Seeduplex 新版 API 与本机 Function Calling

- 2026-07-20 在迁移前快照 `version_backups/2026-07-20_pre_seeduplex/` 的基础上，将主项目语音客户端从旧版实时对话协议迁移到新版 Seeduplex 全双工接口：`wss://openspeech.bytedance.com/api/v3/duplex/realtime/dialogue`，固定模型为 `1.2.6.0`。鉴权只使用本机 `sdkconfig` 中的新版 `X-Api-Key`；API Key 不写入源码、`sdkconfig.defaults`、日志或备份说明，旧 App ID、Access Key、App Key 和旧模型配置已从活动工程移除。
- `main/volcengine_voice.c` 现按新版 JSON WebSocket 流程发送 `session.create`、`session.update`、`input_audio_buffer.append`、`input_audio_buffer.commit`，接收 `session.created/updated`、ASR、TTS 和 `response.done` 事件。麦克风仍以 16 kHz PCM 上行，云端 24 kHz PCM16 下行在 PSRAM 缓冲中流式重采样为现有功放链路使用的 16 kHz，主动说话关闭；保留游玩页按住右键录音、松开提交、最长 6 秒、断线重建和离线提示。
- 语音会话注册 `display_connection_hint` 原生 Function Calling 工具，字段为 `enabled`、`from_port`、`to_port`。每轮要求模型调用一次；没有可靠建议时返回 `enabled=false` 和两个 `-1`。设备端执行前验证端口范围、输出到输入方向、两端有效积木、不同积木以及两端当前空闲，合法建议的两个 WS2812 端口绿色闪烁 8 秒（250 ms 半周期）；退出游玩页、开始下一轮或超时自动清除。工具结果按 `conversation.item.create`、`role=tool` 和原始 `call_id` 回传模型。
- 注入的儿童向提示词要求只针对玩家当前问题和电路给方向性建议，最多 30 个汉字，不复述完整电路，不向玩家说槽位号、P 编号、物理方向或开发术语；连线描述统一使用“某积木的输出端连接到某积木的输入端”。工具事件兼容官方示例中的数组/单对象 `items` 以及顶层/`function` 子对象字段。
- 为兼容 ESP-IDF 5.5.3 与 `esp_wifi_remote` 组件的 Kconfig 版本入口，新增 `managed_components/espressif__esp_wifi_remote/Kconfig.idf_v5.5.3.in`，转接组件已有的 `v5.5` 配置，恢复 ESP32-C6 Hosted Wi-Fi 的全部缓冲和从芯片选项；不改变 C6 通信参数。
- 使用 ESP-IDF 5.5.3 完整构建通过：应用镜像 `0x24F490`（约 2.31 MiB），最小应用分区剩余 `0xDA0B70`（约 86%）；`idf.py size` 显示外部 RAM 使用约 2.18 MiB，DIRAM 使用约 61.8%。活动工程静态检查未发现旧版语音配置。随后已烧录 `/dev/ttyACM0`，bootloader、分区表和应用镜像写后哈希均校验通过。
- 2026-07-20 首次烧录后出现界面和按键卡死并持续打印任务看门狗。串口确认构建配置曾错误回退为 ESP32-H2 + SPI，`spi_trans` 长时间占用 CPU0，且错误地通过 GPIO12 复位从机；故障属于 ESP-Hosted 软件配置回归，不是按键、C6 硬件或主业务逻辑损坏。
- 已恢复 ESP32-P4 Function EV Board 的固定联网链路：协处理器 ESP32-C6、4-bit SDIO 40 MHz、CMD=`GPIO19`、CLK=`GPIO18`、D0-D3=`GPIO14/15/16/17`、C6 RESET=`GPIO54`。`main/CMakeLists.txt` 增加构建期断言，目标、传输方式、总线宽度、频率或任一固定引脚不匹配时直接终止构建，防止错误配置再次静默烧录。
- 修复版使用 ESP-IDF 5.5.3 完整构建并烧录 `/dev/ttyACM0`，应用镜像 `0x256D70`，15 MiB 应用分区剩余约 85%，镜像写后哈希校验通过。串口连续观察约 90 秒：SDIO 明确以 4-bit/40 MHz 初始化并识别从机 `esp32c6`，热点最终获取 IP，DNS、TLS、新版 Seeduplex WebSocket 及 `session.created` 均成功；红外约 4.55 Hz、I2C、WS2812、音频和 UI 健康检查持续运行，未再出现 `spi_trans`、任务看门狗、panic 或循环重启。

## Seeduplex 单轮请求停在 session.update

- 2026-07-20 在不触发 USB 复位的直接串口监听中，两次复现“云端没有回应”。每次进入游玩页并触发右键语音请求后，设备都成功发送 `session.update`，当前电路提示词长度约 2197 字节，内部/DMA/PSRAM 堆余量正常；服务端没有返回 `session.updated`，因此 `begin_capture_after_update()` 没有执行，麦克风云端录音、音频上传、ASR 和模型回复都尚未开始。
- 固件等待更新确认超时后设置重连请求并打印 `recreating Seeduplex WebSocket: transport error`。随后 DNS/TLS 无需重新初始化，WebSocket 证书验证、连接以及新的 `session.create`/`session.created` 均立即成功，证明当前 API Key、热点、C6 SDIO、TLS 和初始会话鉴权链路可用。故障边界位于“已创建会话内的 `session.update` 请求/确认”协议阶段，不是按键未触发、麦克风无数据或设备循环重启。
- 该现场捕获时尚未修改协议代码；随后完成的主机 A/B、状态机修复和构建结果见下一节。

## Seeduplex 单轮录音启动修复

- 2026-07-20 使用本机 `sdkconfig` 中的同一 API Key 对官方 Seeduplex 地址完成最小主机 A/B，测试过程不打印或保存密钥。`session.create` 正常返回 `session.created`；当前固件结构的 `session.update`、只含 `instructions` 的更新，以及约 2197 字节提示词加完整 `display_connection_hint` 工具定义的会话均稳定返回 `session.updated`，确认模型 `1.2.6.0` 支持该事件，报文结构、提示词长度和工具注册不是故障原因。
- 顺序测试在收到 `session.created` 后连续发送 `session.update`、`input_audio_buffer.append` 和 `input_audio_buffer.commit`，服务端正常接受，并实际先返回 `input_audio_buffer.committed`、后返回 `session.updated`。这证明同一 WebSocket 上的消息顺序足以保证更新先于音频进入服务端，`session.updated` 不能作为本地开始采集的硬前置条件。
- `main/volcengine_voice.c` 已改为：`session.update` 成功写入 WebSocket 后立即启动麦克风采集；迟到的 `session.updated` 只记录确认，不重复初始化麦克风。原 `s_turn_update_pending` 及其 20 秒等待分支已移除。发送失败、服务端 `error`、音频上传失败和响应超时仍沿用原有错误提示与 WebSocket 重建路径。
- 修改前快照位于 `version_backups/2026-07-20_pre_session_update_fallback/`。ESP-IDF 5.5.3 完整构建通过，应用镜像 `0x256D10`，15 MiB factory 分区剩余约 85%，C6+4-bit SDIO 构建保护通过。
- 随后 `/dev/ttyACM0` 在主机权限下可用，已将 `0x256D10` 镜像完整烧录到 ESP32-P4 v1.3；16 MiB Flash、bootloader、分区表和应用镜像写后哈希均校验通过。使用不触发 USB 复位的串口直读观察约 35 秒，麦克风持续采样，Seeduplex 发生一次传输自恢复后重新完成证书校验、WebSocket、`session.create` 和 `session.created`，未出现 panic、任务看门狗或异常复位。本窗口没有人工触发右键，完整单轮语音和 Function Calling 仍待交互验证。

## Seeduplex TTS 卡顿与回合状态恢复

- 2026-07-20 针对“回答播放两三秒后中断、界面长期停在提示来了、不能立即再次提问”检查确认两个本地恢复缺口：云端若只到达 `response.done` 而缺少 `response.output_audio.done`，原状态机不会结束播放回合，只能等待 30 秒响应超时；播放环形缓冲首次启动和网络饿死后的恢复又共用 8192 字节水位，短暂断流后会产生较长静音等待。
- `main/volcengine_voice.c` 将 24 kHz 转 16 kHz 后的 PCM 固定拆成 2048 字节、约 64 ms 的播放块，避免把大块音频直接交给功放任务；`response.done` 现在会将仍活动的播放标记为收尾并完整排空队列，再释放本轮状态。协议错误和响应超时会立即中止播放、清理录音/等待标志并重建连接，不再留下无法再次触发右键的半完成回合。
- `main/audio_self_test.c` 保留首次播放 8192 字节、约 256 ms 的抗抖预缓冲；若播放中真正发生过断流，恢复水位降为 4096 字节、约 128 ms，减少网络恢复后的静音时间。修改前快照位于 `version_backups/2026-07-20_pre_playback_recovery_fix/`。
- ESP-IDF 5.5.3 完整构建和 `/dev/ttyACM0` 烧录均成功，应用镜像 `0x2570B0`，15 MiB factory 分区剩余约 85%，bootloader、分区表和应用镜像写后哈希全部通过。烧录后启动串口确认 ESP32-P4 v1.3、16 MiB Flash、ESP32-C6、4-bit SDIO 40 MHz、Wi-Fi、DHCP、DNS、TLS、Seeduplex WebSocket、`session.create/session.created` 和麦克风任务正常，没有 panic、任务看门狗或异常复位；观察窗口没有人工触发新的右键回合，TTS 连续性和收尾恢复仍需按键实测确认。

## Seeduplex Function Calling 回合阻塞与 WebSocket 分片修复

- 2026-07-20 后台串口完整捕获复现：录音、上传、VAD 和 ASR 正常，`你好，你是谁？` 已被识别；随后没有可见的 TTS/Function Calling 收尾事件，约 30 秒后触发 `recreating Seeduplex WebSocket: transport error`。同 API、模型和音色的主机 A/B 能正常收到多段音频、`response.output_audio.done` 和 `response.done`，排除鉴权、模型、音色及必须额外发送 `response.create` 的可能。
- 删除固件自创的 `s_tool_followup_pending` 二次等待状态。`conversation.item.create` 工具结果成功回传后不再重新占用回合；`response.output_audio.done` 现在只标记音频流结束并让播放队列自然排空，`response.done` 无条件作为当前响应唯一终态并恢复右键提问。这样 Function Calling 不会再把已结束的响应阻塞到 30 秒超时。
- WebSocket 接收器不再把每个 frame 当作一条完整 JSON。现按 `payload_offset/payload_len` 拼接 frame 内数据，并跨 TEXT/CONTINUATION frame 累计整条消息；只有当前 frame 收全且 `fin=1` 才交给 JSON 事件处理。乱序 continuation、新 TEXT 抢占未结束消息、非法长度、累计超过 128 KiB 或内存分配失败都会输出明确日志并请求重连，不再静默丢弃后续控制事件。
- 新增每轮低开销诊断统计：最后协议事件、TTS delta 数、Base64 字节、解码后 24 kHz PCM 字节、16 kHz 入队/丢弃字节、Function Calling 收到数、工具结果回传数及关键状态标志。控制事件类型会写入串口，但音频正文和 API Key 不会记录；正常 `response.done`、协议错误和响应超时均输出汇总。
- 修改前快照位于 `version_backups/2026-07-20_pre_tool_turn_state_fix/`。ESP-IDF 5.5.3 完整构建通过，应用镜像 `0x257AB0`，15 MiB factory 分区剩余约 85%；`idf.py size` 显示外部 RAM 使用约 2.32 MiB、DIRAM 使用约 356.7 KiB（61.87%）。固件已完整烧录 `/dev/ttyACM0`，ESP32-P4 v1.3、16 MiB Flash、bootloader、分区表和应用镜像写后哈希均校验通过。启动串口确认 C6 Wi-Fi、DHCP、DNS、TLS、Seeduplex WebSocket 与 `session.create/session.created` 正常，没有 panic 或任务看门狗；常驻后台串口监视已恢复，连续两轮语音交互仍待人工按键验证。

## Seeduplex 音频提交回归修复

- 2026-07-20 对 `0x257AB0` 实机连续两轮复现后，新增统计明确排除本地播放链路：第一轮 ASR 完整识别“你好，你是谁？”，云端随后只发送 5 个 TTS delta；Base64 共 111,964 字节，解码得到 83,968 字节 24 kHz PCM，重采样后的 55,976 字节 16 kHz PCM 全部成功入队，丢弃为 0。最后事件停在 `response.output_audio.delta`，服务端没有继续发送 `output_audio.done`、Function Calling 或 `response.done`，30 秒后本地响应超时。第二轮在 ASR delta 阶段出现 `esp_transport_write() returned 0`，属于 C6 Hosted 上行 socket 在 1 秒内不可写，不是扬声器、Base64 或播放缓冲错误。
- 对比迁移初版、项目交接要求和官方 Seeduplex Demo 后确认协议回归：后续加入 1.5 秒 VAD 静音尾音时，误把原有的 `input_audio_buffer.commit` 分支整体替换掉。当前固件松键后停止上行却不提交音频，与官方“停止收音时提交一次”的生命周期不一致；开发日志此前仍写着会发送 commit，实际源码已不再发送。
- `main/volcengine_voice.c` 已恢复松键/最长 6 秒录音结束后的 `input_audio_buffer.commit`，并输出提交时的真实 PCM 字节数；删除 75 个 20 ms 静音包，直接提交以减少 1.5 秒额外判停延迟和 75 次不必要的 WebSocket 写入。音频 append 的单次可写等待由 1 秒放宽到 3 秒，利用 256 KiB 采集环形缓冲吸收短时 C6 Hosted 背压；收发分离锁保持不变，下行事件不会被该等待阻塞。
- 修改前快照位于 `version_backups/2026-07-20_pre_audio_commit_restore/`。ESP-IDF 5.5.3 完整构建通过，应用镜像 `0x257A90`，15 MiB factory 分区剩余约 85%；实机烧录与连续两轮复验待随后完成。

## Seeduplex 纯语音端到端重写

- 2026-07-20 按当前阶段“先跑通新版 API 语音闭环、不加入 Function Calling”的要求，重写 `main/volcengine_voice.c`。活动协议路径只保留固定 `session.create`、按住右键发送 `input_audio_buffer.append`、松键或满 6 秒发送一次 `input_audio_buffer.commit`，以及 ASR、流式 TTS、`response.output_audio.done` 和 `response.done`；删除每轮 `session.update`、电路快照动态提示词、tools、工具结果回传和二次响应状态。
- 新实现使用单一 `IDLE/RECORDING/WAITING` 回合状态。commit 前先停止采集并上传麦克风环形缓冲里已经产生的全部尾帧，避免松键时截掉句尾；收到每个响应事件都会续期 30 秒响应窗口，`response.done` 无条件释放本轮，`session.closed`、协议错误、上传失败和超时统一清理采集/播放状态并重建连接。WebSocket 继续按 `payload_offset/payload_len` 重组大 TEXT/CONTINUATION 消息，24 kHz PCM16 下行继续流式转换为现有 16 kHz 播放链路。
- 删除 `main/volcengine_voice.h` 和 `main/main.c` 中残留的 AI 连接提示灯接口与渲染逻辑。活动源码静态检查不再包含 `session.update`、`display_connection_hint`、`response.function_call_arguments.done`、`conversation.item.create` 或 VAD 静音尾音。
- 固定提示词和屏幕状态只使用现有 16 px 中文子集覆盖的字符，`tools/check_ui_font_glyphs.py` 检查 app UI 16 px、app UI 24 px 和 shooter 16 px 三套字库全部通过。ESP-IDF 5.5.3 完整构建通过，最终应用镜像为 `0x254600`，15 MiB factory 分区剩余约 85%，ESP32-C6 + 4-bit SDIO 40 MHz 构建断言通过。镜像已完整烧录 `/dev/ttyACM0`，ESP32-P4 v1.3、16 MiB Flash、bootloader、分区表和应用镜像写后哈希均校验通过；常驻后台串口监视已恢复。烧录后设备任务持续运行且未见 panic、看门狗或异常复位，但 C6 当前反复报告 Wi-Fi 断开原因 201，配置热点不在范围内，因此本版 `session.created` 和连续两轮语音闭环仍待热点恢复后实测。

## I2C ISR 防御与语音卡顿诊断

- 2026-07-20 加强项目内 `esp_driver_i2c` 覆盖：NACK、SCL 超时或仲裁丢失中断即使同时带有完成/结束标志，也会在 ISR 临界区清除 `contains_read`、`read_len_static` 和 `rx_cnt`，禁止错误事务继续读取 RX FIFO。接收处理器增加事务指针、目标缓冲区和非零长度检查；发现异常时报告 NACK 状态并结束事务，避免再次向 `NULL` 写入。正常读事务及末尾 NACK 处理保持不变。
- `main/audio_self_test.c/.h` 新增线程安全的单回合播放快照：当前/峰值队列、首个音频入队到首次成功扬声器写入的延迟、实际播放标志、underrun 和 rebuffer 次数。队列自然排空只清理运行状态，不会抹掉终态日志所需的统计。
- `main/volcengine_voice.c` 新增 TTS delta 总数、音频跨度和最大分片间隔，并在 `response.done`、音频结束和响应超时时合并播放层统计；不打印音频正文或密钥，也未调整 API 报文、录音时长、缓冲水位和联网参数。
- ESP-IDF 5.5.3 完整构建通过，应用镜像 `0x254930`，15 MiB factory 分区剩余约 85%；`tools/check_ui_font_glyphs.py` 三套字库全部通过；构建期仍确认 ESP32-P4、ESP32-C6、4-bit SDIO、40 MHz 固定联网配置。该镜像已完整烧录 `/dev/ttyACM0`，bootloader、分区表和应用镜像写后哈希均校验通过。常驻后台串口监视已恢复（守护 PID `1587695`，日志 `后台串口监视.log`）；新固件观察约 100 秒未见 panic 或看门狗，语音回合和长时间 I2C 故障仍待人工复现验证。日志内最近的 `ELF file SHA256: 8c9925529` panic 属于本次烧录前的旧镜像，不应计入 `ccade8e392...` 新 ELF。
- `0x254930` 实机复现得到明确卡顿指标：8 个 TTS delta 共生成 `101032` 字节 16 kHz PCM，零丢弃，首次扬声器输出延迟 `56 ms`；但云端分片最大间隔达到 `3154 ms`，本地峰值队列仅 `64168` 字节，发生 2 次 underrun/rebuffer。卡顿来自过早开始播放后撞上云端长空档，不是解码失败、环形缓冲溢出或功放写错误。
- 根据该实测将首次播放水位从 8192 字节提高到 98304 字节（约 3.07 秒），使相同规模回答接近收齐后再连续播放。正常 `response.output_audio.done`/`response.done` 仍会强制播放不足水位的短回答；若服务端继续缺失终态且 4 秒没有新音频，也会兜底启动，避免永久静音。代价是首句等待增加，换取连续性。
- I2C probe 在设置 START/STOP-only 事务前也会清除残留接收状态，覆盖软件超时后直接进入 probe、未经过普通事务初始化的路径。第二轮 ESP-IDF 5.5.3 构建、字形检查和 `/dev/ttyACM0` 烧录均成功，镜像 `0x254A30`、新 ELF SHA-256 前缀 `8f2c2b7c94`；写后哈希校验通过。常驻监视已恢复为守护 PID `1589198`，新启动观察约 57 秒未见 panic 或看门狗，调整后的语音连续性待下一轮按键实测。
- 同一轮还确认服务端在最后一个 TTS delta 后仍不发送 `response.output_audio.done`/`response.done`，原状态机只能等满 30 秒。增加 6 秒音频静默终态：超过本轮实测最大分片空档 `3154 ms` 后仍无新音频，则完整收尾播放、释放右键状态并主动重建会话，避免界面长期停在“提示来了”。最终镜像 `0x254AC0`，ELF SHA-256 前缀 `ce339fd6f6`；完整构建、三套字形检查和烧录哈希校验通过。常驻监视守护 PID 更新为 `1592227`，本次启动观察约 53 秒未见 panic 或看门狗。
- `0x254AC0` 随后连续两轮出现完全无回答：分别上传 `81408` 和 `73728` 字节有效录音，服务端均确认 `input_audio_buffer.committed`，但事件停在 ASR partial，没有 transcription completed、TTS 或 response 终态。对比成功轮次确认成功时通常在松键前已有足够尾部静音使服务端 VAD 完成；立即停止上行后单独 commit 并不能稳定促成判停。
- 在保留 `input_audio_buffer.commit` 的基础上恢复条件式 VAD 尾音：仅 ASR 尚未完成时追加 1.2 秒、38400 字节 PCM 静音；服务端若在追加期间完成识别则立即停止尾音，已完成的回合不追加。commit 日志新增实际 `vad_tail` 字节数。ESP-IDF 5.5.3 完整构建、三套字形检查和烧录通过，镜像 `0x254BD0`、ELF SHA-256 前缀 `0aa6a29790`；常驻串口守护 PID 更新为 `1595852`，烧录后观察约 29 秒未见 panic 或看门狗。

## Seeduplex 按旧版流畅链路重新实现

- 2026-07-20 只读对比可流畅工作的 `version_backups/2026-07-20_pre_seeduplex/` 后确认，旧版不只是使用不同端点：它通过 `input_mod=push_to_talk` 和专用 `EndASR` 明确控制回合，16 kHz 二进制 TTS 直接进入 8 KiB 播放缓冲；当前新版端点则使用服务端自动 VAD、Base64 JSON 和 24 kHz TTS。实测回合中 `ASR completed`、`response.output_audio.started` 均早于松键后的 commit，证明直接把新版自动 VAD 会话套成“松键立即 commit”并不等价。
- `main/volcengine_voice.c` 按新版官方持续上行语义重写为 `IDLE/RECORDING/ENDING/WAITING`。松键或录音满 6 秒后先停止采集并上传环形缓冲尾帧，随后按真实时间每 20 ms 发送一个 640 字节静音帧，直到收到 `conversation.item.input_audio_transcription.completed`；只有连续 2.5 秒仍未自动判停才发送一次 `input_audio_buffer.commit` 作为兜底。若服务端在按键释放前已完成 ASR或开始回答，会立即停止本轮采集且不再发送迟到的 commit。
- WebSocket 事件回调现在只校验并重组 TEXT/CONTINUATION 分片，完整 JSON 的所有权通过 8 项指针队列交给独立 `voice_rx` 任务。Base64 解码、24 kHz 到 16 kHz 流式重采样和播放入队均移出 WebSocket 接收任务；连接代数和处理互斥保证断线重建时旧消息不会污染新会话。每轮统计新增原始音频消息间隔、WebSocket 分片组装耗时和 RX 队列等待耗时，后续串口可直接区分云端/网络空档与板端处理阻塞。
- 纯语音阶段收到 `response.output_audio.done` 即结束播放输入并释放右键状态，`response.done` 仍作为正常终态；回合序号防止上一轮迟到的 `response.done` 终止刚开始的新录音。删除按音频静默 6 秒强制重连的本地终态猜测，协议错误和真正的 30 秒无响应仍会清理状态并重建连接。
- `main/audio_self_test.c` 恢复旧版 8192 字节、约 256 ms 的首播和断流恢复水位，删除 96 KiB 首播、4 秒强制首播和 4 KiB 特殊重缓冲策略。保留的播放统计只观测队列、首播时间和欠载，不再参与额外超时决策。活动源码不再包含固定 1.2 秒 VAD 尾音、`vad_tail`、96 KiB 水位或 6 秒音频空闲结束逻辑。
- ESP-IDF 5.5.3 两次完整构建通过，应用镜像 `0x255160`，15 MiB factory 分区剩余约 85%；ELF SHA-256 前缀 `634056f263`。`idf.py size` 显示外部 RAM 使用 2,311,288 字节，DIRAM 使用 356,826 字节（61.9%）；三套中文字形检查通过，ESP32-P4 + ESP32-C6、4-bit SDIO 40 MHz 固定配置及构建保护未修改。
- 新镜像已完整烧录 `/dev/ttyACM0`，ESP32-P4 v1.3、16 MiB Flash、bootloader、分区表和应用镜像写后哈希均校验通过。常驻直接串口监视已恢复，守护 PID `1601256`、`cat` PID `1601258`，日志继续追加到 `后台串口监视.log`；新固件从约 56 秒运行时间观察至 79 秒以上未见 panic、任务看门狗或异常复位。当前未进入游玩页且未人工按键，完整 ASR/TTS 连续性和连续两轮立即重问仍需实机复现验证。

## 恢复旧版豆包实时语音 API

- 2026-07-20 按要求停止新版 Seeduplex 路线，恢复指定备份 `version_backups/2026-07-20_pre_seeduplex/`。`main/volcengine_voice.c`、`main/audio_self_test.c/.h`、`main/Kconfig.projbuild` 和 `sdkconfig.defaults` 已逐字节匹配备份；本机 `sdkconfig` 中火山语音配置块也已从该备份恢复，密钥值未输出到终端或日志。
- 活动端点恢复为 `/api/v3/realtime/dialogue`。旧版会话重新使用 `StartSession`、`asr.extra.input_mod=push_to_talk`、`end_smooth_window_ms=800` 和松键专用 `EndASR(400)`；上行和下行均为 16 kHz 单声道 PCM，TTS 二进制音频直接进入 8192 字节播放预缓冲，不再包含新版 `session.create`、Base64 JSON、24 kHz 重采样、自动 VAD、静音续流或 commit 状态机。
- 未整体覆盖工程：保留后来独立完成的 `components/esp_driver_i2c` ISR/接收指针防崩溃修复，以及 `main/CMakeLists.txt` 中 ESP32-P4 + ESP32-C6、4-bit SDIO 40 MHz 和固定引脚构建保护。这样只恢复旧语音业务，不重新引入已解决的 I2C 空指针崩溃或错误 Hosted 配置。
- ESP-IDF 5.5.3 执行 `fullclean` 后完整构建通过，应用镜像 `0x2558e0`，15 MiB factory 分区剩余约 85%；ELF SHA-256 前缀 `753653f7e6`。字形脚本报告旧版云端提示词中的 14 个非屏幕字符未收入 UI 字库，这些文字只作为云端 system role/speaking style 发送，不在 LVGL 屏幕渲染，属于该备份既有的静态检查误报。
- 新镜像已烧录 `/dev/ttyACM0`，ESP32-P4 v1.3、16 MiB Flash、bootloader、分区表和应用镜像写后哈希均校验通过。启动串口确认 C6 经 4-bit SDIO 40 MHz 联网、DHCP、DNS、TLS 和旧版 WebSocket 正常，依次出现 `Volcengine WebSocket connected` 与 `Volcengine protocol connection ready`。常驻串口守护已恢复为 PID `1627842`、`cat` PID `1627844`，完整右键录音、EndASR 和流式 TTS 待实机提问验证。

## 旧版 API 播放预缓冲调整

- 2026-07-20 针对旧版实时语音 TTS 播放卡顿，将首次播放水位从 8192 字节（约 256 ms）提高到 98304 字节（约 3.07 秒），覆盖此前实测约 3154 ms 的云端音频分片空档。短回答收到 TTS 结束事件时仍会强制开始播放，不会因不足水位永久等待。
- 首次播放和欠载恢复改为独立水位；首次成功写入扬声器后，后续欠载仍使用 8192 字节（约 256 ms）恢复，避免异常长空档后再次等待约 3 秒。旧版 API 协议、16 kHz PCM、256 KiB 环形缓冲及 I2C 防崩溃补丁保持不变。
- ESP-IDF 5.5.3 完整构建通过，应用镜像 `0x255960`，15 MiB factory 分区剩余约 85%，镜像 SHA-256 为 `06270e38a9a583e76083618553c39fa0d8537c0012f0f473d700729bd896f986`。常规整镜像烧录因本机 USB-Serial/JTAG 长事务在约 2% 处反复失联，改为按连续地址拆成 75 个 32 KiB 以内区块写入；完整 `0x10000` 至 `0x265fff` 地址范围逐块通过设备端哈希校验。硬复位后串口从运行时间约 17 秒连续观察到 41 秒以上，麦克风任务持续采样，未见 panic、任务看门狗或异常复位；播放连续性待游玩页实际语音回合验证。

## 旧版 API 播放卡顿诊断埋点

- 2026-07-20 在 `version_backups/2026-07-20_pre_old_api_playback_diagnostics/` 备份修改前的 `main/audio_self_test.c/.h`、`main/volcengine_voice.c/.h` 和本日志。此次只增加观测数据，没有修改 96 KiB 首播水位、8 KiB 欠载恢复水位、256 KiB 播放环形缓冲、WebSocket 音频包写入粒度、I2S/DMA 配置或音频任务优先级。
- 旧版火山 TTS 入流新增会话统计，在 `TTSEnded` 输出一次，若该事件缺失则在 `SessionFinished` 兜底输出一次。`TTS ingress summary` 包含音频包数、PCM 总字节数、包长最小/最大值、首包至末包跨度和最大包间隔，可直接判断云端或网络侧是否存在长空档。
- 扬声器路径新增播放统计：当前/峰值排队字节数，入队和成功写入的包数/字节数，写失败数，首包入队至首次真实 PCM 写入延迟，欠载/重缓冲次数，单次 `esp_codec_dev_write()` 最大耗时，相邻成功 PCM 写入最大间隔，写入尺寸范围、任务亲和核和实际运行核掩码。自然排空后输出一次 `voice playback summary`；自然结束和中止只清队列状态，完整统计保留到下一次 `playback_begin` 才重置。
- ESP-IDF 5.5.3 增量构建通过，应用镜像 `0x256080`（2,449,536 字节），15 MiB factory 分区剩余约 85%，SHA-256 为 `294f05297afe17349d0edaa36844fa33a615bcf21bfda20886bf7a43872b3d57`。镜像从 `0x10000` 起拆成 75 个连续区块写入，前 74 个为 32 KiB，末块有效数据 24,704 字节；所有区块均通过设备端哈希校验，末块擦写覆盖至 `0x266fff`，随后硬复位成功。
- 启动串口确认 `speaker task priority=6 affinity=-1`，音频初始化、旧版 Volcengine WebSocket 连接和协议连接均成功，本次启动未见 panic 或任务看门狗。常驻串口监视已恢复为独立会话，父 PID `1641231`、读取 PID `1641234`，日志继续追加到 `后台串口监视.log`；下一步需实机连续完成至少两轮语音，结合两类 summary 决定是否进入固定块写入和本地负载 A/B。

## 旧版 API 1024 字节固定块播放 A/B

- 2026-07-20 两轮诊断语音均收到约 121 KiB PCM，播放队列峰值覆盖全部 PCM，`underrun=0`、`rebuffer=0`、`errors=0`，排除应用层缺数。两轮最大网络包间隔分别为 539,259 us 和 583,682 us；最大 `esp_codec_dev_write()` 耗时却分别达到 597,868 us 和 585,439 us，而最大 14,336 字节包只对应 448 ms 音频，卡顿范围缩小到板端 I2S 写入/任务调度。
- 修改前版本已备份到 `version_backups/2026-07-20_pre_fixed_block_playback/`。本轮只将 WebSocket 变长 PCM 连续重组为 1024 字节播放块，整段语音仅最后一块允许不足 1024 字节；96 KiB 首播、8 KiB 重缓冲、256 KiB 环形缓冲、I2S/DMA 配置以及 IR/I2C 行为均未修改，扬声器任务仍保持优先级 6 且不绑核。
- 播放诊断新增所有 codec 调用累计耗时、首次写入至末次写入完成的跨度、按 16 kHz PCM16 计算的理论时长及二者差值。固定块 A/B 后的 `write` 项数应从原来的 11 个网络包增加到约 119 个播放块，`max_write_size` 应为 1024 字节；这些指标用于决定下一步是否单独将扬声器优先级从 6 提高到 8。
- ESP-IDF 5.5.3 增量构建通过，应用镜像 `0x256430`（2,450,480 字节），SHA-256 为 `0bbf8b10c966dd4e253b188f2751e2c9f965440cac70da6c31f811f9f07ff695`。镜像从 `0x10000` 起拆成 75 个连续区块写入，所有区块均通过设备端哈希校验，随后硬复位成功。
- 启动串口确认 `voice write block=1024 bytes`、`speaker task priority=6 affinity=-1`，音频初始化及旧版 Volcengine WebSocket/协议连接成功，未见 panic 或任务看门狗。常驻串口监视已恢复为独立会话，父 PID `1644823`、读取 PID `1644826`，继续追加 `后台串口监视.log`；固定块版本仍需实机完成两轮语音 A/B。

## 旧版 API 扬声器优先级 A/B

- 2026-07-20 固定块优先级 6 版本完成两轮实测，仍能听到卡顿。两轮分别播放 101,832 和 108,736 字节 PCM，均保持 `underrun=0`、`rebuffer=0`、`errors=0`，实际写入项数为 100 和 107，除最后一块外均为 1024 字节，确认固定块重组正确。
- 1024 字节在 16 kHz PCM16 下理论时长仅 32 ms，但两轮单次 codec 写入最大耗时分别达到 217,644 us 和 217,663 us。整段写入跨度分别为 4,368,732 us 和 4,335,992 us，对应理论时长 3,182,250 us 和 3,398,000 us，额外耗时分别为 1,186,482 us 和 937,992 us；播放仍只在 Core 1，应用队列未欠载，进一步确认是低优先级扬声器任务不能及时补充 I2S DMA。
- 优先级修改前版本已备份到 `version_backups/2026-07-20_pre_speaker_priority_8/`。本轮只将 `AUDIO_SPEAKER_TASK_PRIORITY` 从 6 提高到 8，使其高于包含 2.4 ms 槽位忙等的优先级 7 IR 扫描任务；1024 字节固定块、任务不绑核、预缓冲、I2S/DMA、IR/I2C 及 Hosted 配置均保持不变。
- ESP-IDF 5.5.3 增量构建通过，应用镜像仍为 `0x256430`（2,450,480 字节），SHA-256 为 `988b2d2ec6f7ec9182243f63c7af9a26951dd7db3f35940ebd360df3e4fb3046`。75 个连续烧录区块全部通过设备端哈希校验，硬复位后串口确认 `voice write block=1024 bytes`、`speaker task priority=8 affinity=-1`，旧版 Volcengine WebSocket/协议连接成功，未见 panic 或任务看门狗。
- 常驻串口监视已恢复为独立会话，父 PID `1646373`、读取 PID `1646376`，继续追加 `后台串口监视.log`。下一步需在相同游玩负载下完成两轮语音，比较最大 1024 字节写入耗时和整段 `overhead` 是否显著下降。

## 保留现有逻辑迁移至 Seeduplex 新版 API

- 2026-07-20 在修改前创建 `version_backups/2026-07-20_pre_seeduplex_protocol_adapter/`，保存语音源文件、音频层、Kconfig、`sdkconfig.defaults`、本机 `sdkconfig` 和开发日志。此次只替换云端协议接口，不加入 Function Calling、连线提示、VAD 静音尾音、新 UI 状态或额外业务流程。
- 保留游玩页按住右键录音、松键结束、最长 6 秒、每轮动态注入关卡/积木/连接信息以及原有错误与重连行为。旧 `StartConnection` 映射为新版 WebSocket 鉴权和 `session.create`，每轮 `StartSession` 映射为携带原提示词的 `session.update`；16 kHz PCM 上行改为 Base64 `input_audio_buffer.append`，松键排空采集尾帧后只发送一次 `input_audio_buffer.commit`。
- 新版下行 24 kHz Base64 PCM16 使用连续 3 输入采样到 2 输出采样的流式重采样送入现有 16 kHz 播放接口；WebSocket TEXT/CONTINUATION 分片先完整重组再解析。`response.output_audio.done` 结束播放输入，`response.done` 结束当前回合。活动源码不包含 `display_connection_hint`、Function Calling、`conversation.item.create` 或 VAD 尾音。
- `main/audio_self_test.c/.h` 与迁移前备份逐字节一致，因此继续使用 96 KiB 首播、8 KiB 欠载恢复、1024 字节固定播放块、256 KiB 环形缓冲和优先级 8 扬声器任务；现有播放诊断、I2C 防崩溃补丁、红外逻辑及界面状态均未修改。
- 新版端点为 `/api/v3/duplex/realtime/dialogue`，ESP-Hosted 保持 ESP32-C6、Function Board、4-bit SDIO 40 MHz。首次实机握手返回 HTTP 401，确认是恢复旧 API 时本机 `sdkconfig` 同时恢复了旧接口凭据；已从当天早先成功连接新版接口的本机构建记录恢复 36 字符 Seeduplex API Key，密钥值未写入源码、默认配置或开发日志。
- ESP-IDF 5.5.3 在正确的 `ESP_IDF_VERSION=5.5` Kconfig 入口下完整构建通过，最终应用镜像 `0x2570B0`（2,453,680 字节），15 MiB factory 分区剩余约 85%；BIN SHA-256 为 `0e246e63451ecea474c920a9116f90f64dac0012aa644caa7d47fe1f4ad28bef`。镜像拆成 75 个连续 32 KiB 以内区块写入 `/dev/ttyACM0`，所有区块均通过设备端哈希校验。
- 硬复位后串口确认 ELF SHA-256 前缀 `dd884c1e1`、扬声器任务优先级 8、1024 字节固定播放块、ESP32-C6、4-bit SDIO 40 MHz、Wi-Fi、DNS、TLS、新版 WebSocket 和 `session.created` 全部正常；持续观察未见 panic、任务看门狗或异常复位。常驻串口监视已恢复，父 PID `1674035`、读取 PID `1674038`，日志继续追加到 `后台串口监视.log`；完整录音/ASR/TTS 和卡顿情况待人工连续测试两轮。

## Seeduplex 音频 append 背压超时修复

- 2026-07-20 三轮实机均确认“语音上传失败”的直接来源是本地 WebSocket 可写等待超时：`session.update` 和多包音频已经成功发送、服务端甚至已返回 ASR started/delta，随后 `transport_poll_write(0)` 与 `esp_transport_write() returned 0` 触发上传失败和连接重建；服务端没有返回上传格式或鉴权错误。最近一次从 `544585 ms` 开始采集，在 `548172 ms` 再次进入同一失败分支。
- 另一次测试成功上传并 commit `72192` 字节，服务端确认 `input_audio_buffer.committed`，但事件停在 transcription delta 并于 30 秒后响应超时。这是独立的 ASR 回合结束问题，不能用 append 写超时解释；本轮只修复已重复确认的上传失败，不修改 commit、VAD、录音和播放状态机。
- 修改前快照位于 `version_backups/2026-07-20_pre_seeduplex_tx_timeout_3s/`。仅将新版 Base64 `input_audio_buffer.append` 的 WebSocket 可写等待从 1000 ms 调整为 3000 ms；256 KiB 采集环形缓冲继续吸收等待期间的麦克风数据，其余协议和项目逻辑不变。
- ESP-IDF 5.5.3 增量构建通过，应用镜像 `0x2570B0`（2,453,680 字节），BIN SHA-256 为 `c8e436a06c6da38190060a4d320d8d05350f31ad6614826b9858738d2131a40f`。75 个连续烧录区块全部通过设备端哈希校验；硬复位后串口确认 ELF SHA-256 前缀 `d9a116a3a`、优先级 8、1024 字节播放块、ESP32-C6、4-bit SDIO 40 MHz、Wi-Fi、DNS、TLS、Seeduplex WebSocket 和 `session.created` 正常。常驻串口监视父 PID `1678053`、读取 PID `1678056`。

## 回退 append 超时并确认新版 ASR 判停差异

- 3 秒版本实测直接证伪“只因 1 秒等待太短”：一轮在 `135023 ms` 开始采集，`138306 ms` 仍以 `transport_poll_write(0)` 失败，等待长度只是从约 1 秒延长到约 3 秒；另两轮分别完整上传并 commit `84480`、`76800` 字节，服务端均确认 `input_audio_buffer.committed`，却没有 transcription completed、TTS 或 response 事件，30 秒后本地超时。故障主线不是单次 socket 超时。
- 本机已下载的官方 `web_duplex_demo` 明确说明新版 Seeduplex 是“全双工持续上行”：连接后持续发送 16 kHz PCM，麦克风静音时每 20 ms 发送一个 640 字节静音帧；`input_audio_buffer.commit` 只是自动 VAD 迟迟不回复时的“强制判停”或停止实时收音时的补充事件。活动固件却把旧版 push-to-talk 的松键语义直接映射成“停止所有 append 后立即 commit”，两者并不等价。
- 实机事件与该协议差异一致：服务端能产生 transcription started/delta，证明 PCM 格式、Base64 和上行数据有效；commit 也被确认，证明事件已接收；唯独停止上行后没有足够尾部静音供服务端 VAD产生 completed。历史新版成功回合也只出现在松键前已有自然静音，或客户端额外续送静音时。
- 按要求已将 `VOICE_AUDIO_SEND_TIMEOUT_MS` 从 3000 ms 回退为 1000 ms。重新构建得到原始回退镜像 `0x2570B0`，BIN SHA-256 恢复为 `0e246e63451ecea474c920a9116f90f64dac0012aa644caa7d47fe1f4ad28bef`，ELF SHA-256 恢复为 `dd884c1e1132789ee7882e6ce2001c4d301b517470845aee886865ed95f4576d`；75 个烧录区块全部通过哈希校验。启动串口确认 C6/SDIO、Wi-Fi、TLS、Seeduplex WebSocket 和 `session.created` 正常，常驻监视父 PID `1680277`、读取 PID `1680280`。
- 对照官方新版协议文档 `https://www.volcengine.com/docs/6561/2549732` 确认遗漏字段：新版 `input_audio_buffer.commit` 对应旧版 `EndASR`，但只有在 `session.create.extension.dialog.extra` 设置 `input_mod: "push_to_talk"` 时才会屏蔽服务端 VAD、由客户端结束音频。当前 `create_session_extension()` 只迁移了主动说话/响度/音乐开关，没有迁移该模式字段；因此服务端仍按 `server_vad` 处理，表现为 `input_audio_buffer.committed` 后停在 transcription delta。下一次修复应只补这个协议配置，并保留现有松键、commit、录音和播放逻辑。

## Seeduplex push-to-talk 配置补全

- 2026-07-21 重新核对火山引擎官方“接入必读”与全双工 API 文档：新版 `session.extension` 内的 `asr/tts/dialog` 应直接复用旧 `StartSessionPayload`；`input_audio_buffer.commit` 明确映射旧 `EndASR`；麦克风按键模式必须在 `extension.dialog.extra` 配置 `input_mod=push_to_talk`，才能屏蔽服务端 VAD并由客户端 commit 判停。新版音频格式示例继续使用 `rate=16000/24000`，无需改成 Web Demo 中的 `sample_rate`。
- 修改前快照位于 `version_backups/2026-07-21_pre_push_to_talk_extension/`。`create_session_extension()` 仅新增 `dialog.extra.input_mod="push_to_talk"`，并把旧项目原有的 `asr.extra.end_smooth_window_ms=800` 迁入新版 extension；append 发送等待保持回退后的 1000 ms，不加入静音尾音，不改变按键、最长 6 秒、单次 commit、动态提示词、UI、I2C、红外和播放层。
- ESP-IDF 5.5.3 增量构建通过，应用镜像 `0x257160`（2,453,856 字节），BIN SHA-256 为 `e758e1ce4b11abe131018aa1b33c458923c0d13538b002429aeb99affa4cecf0`，ELF SHA-256 为 `31c9bb3f2880b0e5a8d0b02a42fe2f4fb952aa0dfeb7c853806e76ddcf2ea7ab`。75 个烧录区块全部通过设备端哈希校验。
- 启动串口确认优先级 8、1024 字节播放块、ESP32-C6、4-bit SDIO 40 MHz、Wi-Fi、DNS、TLS、Seeduplex WebSocket 和 `session.created` 正常。空闲观察超过 126 秒，先前约 71 秒固定出现的 `AudioServerNoAudioInputTooLongError` 未再发生，证明服务端已实际采用 push-to-talk 模式。常驻串口监视父 PID `1682406`、读取 PID `1682409`；完整按键录音、commit、ASR completed 和 TTS 待人工回合验证。
## Seeduplex Push-to-Talk 迁移实测失败

- 2026-07-21 在新版 `/api/v3/duplex/realtime/dialogue` 的 `session.create.extension` 中补充旧版 `asr.extra.end_smooth_window_ms=800`、`asr.extra.input_mod=push_to_talk` 和 `dialog.extra.input_mod=push_to_talk` 后，ESP-IDF 5.5.3 构建与 75 个应用分片烧录校验均成功。
- 实机多轮日志确认音频上行本身正常：最近一轮发送 `87040` 字节 PCM，服务端返回 `input_audio_buffer.committed`，随后返回 `conversation.item.input_audio_transcription.started` 和一次 `delta`；但始终没有 `transcription.completed`、TTS delta 或 response 终态，30 秒后本地超时重连。另两轮 `89600` 和 `86016` 字节 PCM 表现相同。
- 因此故障不在麦克风、Base64、WebSocket 上传、commit 接收或播放链路，而在新版服务端没有完成 ASR 判停。迁移旧版 `asr.extra.input_mod` 并不能让新版 Seeduplex 复制 `StartSession/EndASR` 的 Push-to-Talk 语义；即使同时设置 `dialog.extra.input_mod`，服务端实测仍按持续上行/服务端 VAD 行为处理。
- 官方 `web_duplex_demo` 的实际实现同样不使用 `input_mod=push_to_talk`：连接期间持续发送 16 kHz PCM，静音时每 20 ms 发送 640 字节静音帧，`input_audio_buffer.commit` 仅用于自动 VAD 迟迟不结束或停止实时收音时的强制判停。当前固件松键后停止所有 append 并立即 commit，与新版接口的持续上行契约不一致，这就是 ASR 停在 partial delta、没有 TTS 的直接原因。
- 本轮只记录结论，不继续修改新版状态机。按现场要求恢复并烧录 `version_backups/2026-07-20_pre_seeduplex_protocol_adapter/` 中已完成播放优化的旧版 API：旧 `StartSession/EndASR`、16 kHz PCM、96 KiB 首播、8 KiB 欠载恢复、1024 字节固定写入和扬声器任务优先级 8。
- 旧版备份的 `volcengine_voice.c`、`Kconfig.projbuild`、`sdkconfig.defaults` 和本机 `sdkconfig` 已逐字节恢复；`audio_self_test.c/.h` 原本已与该备份一致，因此没有重复覆盖。ESP-IDF 5.5.3 `fullclean` 构建通过，应用镜像 `0x256430`（2,450,480 字节），BIN SHA-256 为 `5d586c736aa0b213e8b373058c19b1f4b06f16f2667e09dab94c4d61b0980391`，ELF SHA-256 为 `9c71ae358e5d7476d0d0d19ac4b4a98421fa84f009a09f3f6a25a39102cdb89e`。bootloader、分区表及 75 个连续应用分片全部通过设备端哈希校验；硬复位后 Wi-Fi、DNS、TLS、`Volcengine WebSocket connected` 和 `Volcengine protocol connection ready` 正常，持续观察未见 panic、看门狗或异常复位。后台串口监视已恢复。

## 完整关卡云存档上传

- 2026-07-21 新增 `progress_sync.c/.h`。设置页的“云端同步 / 上传云存档”会读取本机内存中的真实通关状态，按 `device-progress-sync-api.md` 的固定白名单生成完整 16 关快照；明确排除初始关 `101` 和所有奖励关，且不受本次上电的临时全关解锁影响。
- 上传使用 HTTPS `POST`、`Authorization: Bearer`、JSON `schemaVersion=1`，以独立 FreeRTOS 任务执行，不阻塞 UI。仅 HTTP 200、`success=true` 且 `data.levelCount=16` 视为成功；网络错误、超时和 HTTP 500 按 1 秒、3 秒、10 秒最多重试三次，其他服务端错误不重试，本地 NVS 始终不被此操作修改。成功响应会回传完整快照，因此接收缓冲定为 4 KiB，避免原 1 KiB 缓冲截断响应后误判失败。
- 设置卡片展示上传中、成功、离线、密钥未配置、服务端拒绝和失败状态；动态状态位于卡片下方，未与静态说明重叠。24 px 标题字库补充“云端同步 / 上传云存档”，16 px 状态字库补充缺字；`tools/check_ui_font_glyphs.py` 三套字库均通过。
- ESP-IDF 5.5.3 完整构建和 `/dev/ttyACM0` 烧录成功。镜像为 `0x2571A0`（2,454,944 字节），最小应用分区剩余 `0xD98E60`（85%）；BIN SHA-256 为 `b203ffb28f660666c0475736b9c4f70698042c677b579772e163b6964d1dfe99`，bootloader、分区表和应用镜像均通过设备端哈希校验。烧录后无复位方式的串口守护已独立运行并追加 `后台串口监视.log`。
- 本机 `sdkconfig` 已启用同步功能、配置目标 URL 和设备同步密钥；密钥仅保存在本机配置与已烧录镜像中，不写入源码、默认配置或本日志。密钥配置版已重新完整构建、烧录并通过设备端写后哈希校验，等待从设置页触发首次实际上传验证。
- 首次上传实测在请求开始后触发 `progress_sync` 任务的 Stack protection fault 并重启。根因是任务栈仅 6 KiB，而 `upload_snapshot()` 在栈上保留完整 4 KiB HTTP 响应缓冲，叠加 cJSON 格式化调用的 `sprintf` 栈帧后越界。已将响应缓冲改为堆分配并在所有返回路径释放，同时将该任务栈提高到 8 KiB；协议、重试、UI 和本地存档语义未改。修复版已构建、烧录并通过设备端哈希校验，后台串口守护已恢复。

## C6 三热点信号优选

- 2026-07-21 在修改前创建 `version_backups/2026-07-21_pre_multi_ap_wifi/`，保存 C6 联网源码、Kconfig、本地构建配置、交接文档和开发日志。
- `main/c6_network_test.c` 由单一 SSID 直连改为三组已配置热点的扫描选择：首次联网、连接超时和每次断线后均扫描可见 AP，只在已配置的三组中选择 RSSI 最高者，再应用对应凭据连接。密码不写入串口日志。
- Wi-Fi 事件回调不再直接重连；它只标记重连请求并通知 `c6_wifi_test` 任务。该任务串行执行扫描和连接，避免在事件回调内进行阻塞扫描。连续八次未找到或未连上配置热点会记录失败并重置重试窗口，后续继续按扫描结果恢复。
- 新增两组备用热点 Kconfig 项及本机构建配置；未配置的备用 SSID 自动忽略。原主热点保持为候选之一，不再固定优先。
- ESP-IDF 5.5.3 完整重新配置并构建通过，`c6_network_test.c` 已编译，最终 Ninja 链接完成。应用镜像为 `0x25fb10`，最小应用分区剩余 `0xd904f0`（85%）。本次尚未烧录，现场应验证启动和断线时的“scan selected configured hotspot”日志、RSSI 选择及自动恢复。

## 自建 AI 后端固件接入

- 2026-07-22 在 `version_backups/2026-07-22_pre_ai_backend_firmware/` 保存接入前固件关键源码与本地配置。服务器端已部署 IP-SAN 私有 CA 的 `wss://47.108.57.127:8443/ws/device`，设备信任根以 `main/certs/ai_backend_ca.pem` 嵌入；设备令牌只写入本机 `sdkconfig`，不写入默认配置、源码或日志。
- 新增 `main/ai_backend_voice.c/.h`：独立于旧版 `volcengine_voice` 的 WebSocket v2 客户端，包含设备认证、`session.start`、完整电路快照、16 kHz PCM 二进制上行、`input_audio.commit`、TTS PCM 下行和既有 playback begin/push/finish 接口复用。旧版模块未删除。
- 新增 `main/voice_backend.c/.h`，用独立 NVS 命名空间持久化 `Legacy` 或 `AI` 选择，默认 Legacy。设置页新增 `AI Voice` 卡片，按中键在 `AI / Legacy` 间切换；输入和游玩页语音状态均按当前选择路由，未选中的模块不会接收右键录音。
- 为适配现有 16 px 中文子集，紧凑设置卡使用 ASCII 的 `AI / Legacy` 文案；`tools/check_ui_font_glyphs.py` 三套字库均通过。
- ESP-IDF 5.5.3 完整构建通过，镜像 `0x2610a0`，最小应用分区剩余 `0xd8ef60`（85%）。本次尚未烧录。`highlight_ports` 的 WS2812 非阻塞叠加与 30 次实机回归仍待完成，切到 AI 后端前应先验证 WSS 握手、语音和旧版回退。

## AI 后端关卡语义与提示词对齐

- 2026-07-22 从 `ziyan2006/AI_Backend` 拉取 GitHub 基线（`41d606b`）并与服务器运行版本比对；保留服务器既有的设备令牌认证、管理接口认证、单轮音频上限、管线超时与 TLS/WSS 部署配置。
- 后端新增结构化 `level` 上下文，并将快照转为旧版等价的中文语义：当前关卡目标、输入/输出名称、已识别模块、有效连接和无效连接数量。系统提示词恢复“图灵号电路指导员”、儿童语气、只依据检测结果、30 字以内、一次一条方向性提示和不泄露硬件术语等约束。当前固件未实现端口高亮执行，因此后端暂时禁止模型发起该工具调用，避免回合等待无响应。
- `main/ai_backend_voice.c` 将快照升级为 schema v2：上传关卡编号/目标/输入输出名称，模块的稳定类型与中文显示名，并把有效连线规范为输出模块到输入模块；同时上传无效连接和扫描统计。旧 `gate` 数字字段继续保留，便于兼容。
- 后端生产目录在替换前已备份两处源码；部署后 36 项测试和 Ruff 全部通过。受保护的真实诊断请求以第 101 关、输入积木和输出积木验证，LLM 返回“把输入积木的输出端接到输出积木的输入端”，WSS 服务保持 active。密钥、设备令牌和 Wi-Fi 凭据均未记录。
- 本机当前没有可用设备串口。固件改动尚未烧录：尝试重新配置 ESP-IDF 时，当前依赖的 ESP-Hosted Kconfig 会错误改选 ESP32-H2/SPI，违反项目 C6+4-bit SDIO 构建保护；已从 `version_backups/2026-07-22_pre_ai_backend_firmware/sdkconfig` 恢复 C6/SDIO 配置，并保留 AI 本地配置。需先修复该构建环境或恢复原可用 ESP-IDF Python/Kconfig 组合，再完成构建、烧录与设备端验证。
- 同日已定位并解决构建问题：ESP-Hosted Kconfig 需要 `ESP_IDF_VERSION=5.5`（不能使用 `v5.5.3`）才能保留 C6 + Function Board + 4-bit SDIO 选择。完整 Ninja 构建现已通过，应用镜像 `0x2615a0`（2,495,904 字节），最小应用分区剩余 `0xd8ea60`（85%），esptool 镜像校验哈希有效。
- 串口检查发现主机 USB 枚举不稳定：内核曾多次识别 Espressif USB JTAG/serial debug unit 并创建 `/dev/ttyACM0`，但随后反复出现 USB error -71、断开和无法枚举；本次检查结束时 `/dev/ttyACM0` 与 `/dev/ttyUSB*` 均不存在，未执行烧录或串口监视。需先稳定设备 USB 连接后再烧录该镜像并实测新版关卡上下文。
- USB 重新枚举为 `/dev/ttyACM0` 后，已将 `0x2615a0` 镜像完整烧录到 ESP32-P4 v1.3。设备识别为 16 MiB Flash；bootloader、分区表和应用镜像均完成写入并通过设备端哈希校验，随后执行硬复位。复位后串口节点仍存在且未被其他进程占用；本次监听在启动首屏结束后才打开，未捕获可用于判断联网和 WSS 状态的新启动日志，需在后续一次语音实测时持续抓取。

## AI 语音可靠性、关卡预检与端口高亮

- 2026-07-22 针对新版 AI 语音的四个问题完成固件与后端改造。固件上行快照已补充关卡要求的输入/输出积木数量；后端在调用模型前先作确定性预检，数量不足时直接提示缺少的输入或输出积木，不再给出接线建议。该预检覆盖模型回复不稳定的情况。
- 后端将电路快照转为关卡目标、已识别积木、有效连接和无效连接的中文语义，并将指导语气调整为 45 至 60 个汉字的一至两句自然提示，可使用适合学龄儿童的亲切表达。端口高亮工具重新开放为自动选择，模型需要指向接线位置时可发起请求。
- 固件新增 `device.command/highlight_ports` 接收、参数校验与结果回传；在既有 GPIO46 的 WS2812 渲染任务上叠加非阻塞黄灯闪烁或呼吸效果，支持最多 4 个端口、0.5 至 10 秒。原有连接与调试灯效保持不变。AI 语音录音新增平均绝对振幅静音门限，过静音频不提交 ASR；并以会话建立中状态抑制未就绪期间重复发送 `session.start` 和快照，降低无效请求和服务端 `AsrNoSpeechError`。
- 后端生产目录替换前已备份 `models.py` 与 `openai_compatible.py` 到服务器既有备份目录，随后重启 `tuco-ai-backend.service`，服务状态为 active，监听 `https://0.0.0.0:8443`。配置文件、设备令牌、LLM/火山引擎密钥均未改动或写入日志。生产端完整测试 35 项通过，Ruff 静态检查通过；项目内 `AI_Backend/` 副本已同步同一源码和测试，避免后续维护偏差。
- 本机固件完整构建已通过，镜像为 `0x261ca0`，最小应用分区仍保留约 85% 空间。USB 串口在多次断连后重新枚举为 `/dev/ttyACM0`，已完成 bootloader、分区表和应用镜像烧录；全部写入区段均通过设备端哈希校验，并执行硬复位。复位后 USB 串口再次断开，未能采集启动日志；待串口稳定后实测缺积木优先提示、静音不提交、端口高亮及儿童化回复。
- 随后发现新版 AI 客户端的后端 `error` 分支没有清除 `s_session_starting`。一旦工具回合后的任意错误恰好发生在会话建立阶段，下一次右键会被“会话建立中”条件永久拦截，界面不再进入“正在听你说”。现已在错误和音频完成路径统一清理本轮建立、录音、等待和播放状态，并为 `session.ready` 增加 10 秒建立超时回收，避免任何丢失事件永久锁住输入。修复版构建镜像为 `0x261d30`，应用分区剩余 `0xd8e2d0`（85%）；bootloader、分区表和应用镜像已重新烧录，所有写入区段通过设备端哈希校验并硬复位。

## 新版语音会话稳定性与语音后亮灯

- 2026-07-22 新版 AI 固件的右键启动改为只消费一次按下上升沿；旧实现按按住状态循环判断，播放结束、错误回收或按键采样延迟都可能让同一次按住自动创建下一会话。服务端同步改为只有验证完 `circuit.snapshot` 后才发送 `session.ready`，避免设备在快照尚未到达时开始上行 PCM 并被服务端拒绝。
- 录音统计现在分开记录采集字节和成功上传字节。传输失败会提示“语音上传失败，请再试一次”，不会误报“说得太短”；平均绝对振幅门限由 120 降为 24，最小采集长度仍保留 3200 字节，减少正常说话被误判为静音的概率。短音和上传失败都会关闭本地会话，下一次右键始终从干净会话开始。
- `highlight_ports` 扩展为最多 8 个端口、默认 20 秒、最多 60 秒。后端提供每块积木的四个物理端口映射，并要求涉及“从哪里接到哪里”的提示同时高亮两块积木全部八个端口；玩家明确指定时长时，模型可在允许范围内调整时长。
- 工具回合顺序改为“先朗读提示，再亮灯”：后端先下发 TTS，固件在扬声器队列实际播放结束后回传 `response.audio.played`，服务端才下发端口高亮命令；高亮确认后发送 `response.done` 关闭会话。这样不会出现灯先亮、提示后到的情况，也不会因工具等待锁住下一次右键。
- 后端隔离目录及生产目录完整回归均为 37 项测试通过，Ruff 通过。部署前已备份服务器的相关源文件，服务重启后状态为 active，密钥和配置未改动。固件完整构建通过，镜像 `0x262020`，最小应用分区剩余 `0xd8dfe0`（85%）；bootloader、分区表和应用镜像已烧录并全部通过设备端哈希校验，随后执行硬复位。

## AI 语音 MP3 抓包

- 2026-07-22 后端新增可控语音抓包：设备上传给 ASR 的 PCM 保存为 `input` MP3，后端 TTS 下发 PCM 保存为 `output` MP3，均为 16 kHz 单声道。抓包编码失败只记服务端诊断，不影响语音回合。
- 生产环境已安装 `ffmpeg` 并启用抓包。文件保存到后端 `runtime/audio_captures`，默认最多保留 100 个、7 天后自动删除；目录不公开，需使用管理员令牌通过受保护的抓包列表与下载接口访问，文件名做了路径穿越校验。
- 部署前已备份远端工作区和运行配置。后端测试现为 39 项通过，Ruff 通过；真实 PCM 冒烟测试已生成 MP3，WSS 服务重启后为 active。

## AI 语音按键预采集

- 2026-07-22 通过后端 `input` MP3 定位到设备端截断：一轮实际按键语音仅上传约 2.12 秒 PCM，原因是旧状态机先等待 `session.ready`，收到后才开启麦克风，因此会话建立阶段的说话内容没有被采集。
- `main/ai_backend_voice.c` 现改为右键按下即开启本地采集，利用既有 256 KiB PSRAM 环形缓冲保存最长 6 秒 PCM；后端会话就绪后发送 `input_audio.start` 并依序补发缓冲。按键在就绪前松开时仍会在会话就绪后完成上传和 `commit`，不会丢弃已采集音频。
- 新增串口诊断覆盖预采集启动、`session.ready`、音频开始、停止、上传失败，以及最终 `captured/sent/samples` 统计。完整构建通过，镜像 `0x2623d0`，最小应用分区剩余 `0xd8dc30`（85%）。后续烧录因 USB JTAG/串口节点在 esptool 打开前断开而未开始写入，板上仍是旧镜像；需先稳定 USB 后再烧录并以新增字节统计核对实际语音时长。
## ESP-Claw 最小嵌入与 DeepSeek 对话验证准备

- 2026-07-25 在 `version_backups/2026-07-25_pre_esp_claw_min/` 保存接入前源码与脱敏本机配置。项目内固定 ESP-Claw `06eb576` 的 `claw_core` 与 `claw_utils` 最小副本；裁掉 IM、事件路由、Web 服务、MCP、文件系统、持久记忆、Lua 和动态技能，因此不修改现有分区表。
- 新增 `main/tuco_agent.c/.h` 与串口入口：ESP-Claw Agent Loop 以 DeepSeek OpenAI 兼容接口异步处理文本，注入当前关卡和电路快照摘要；DeepSeek 凭据仅在本机 `sdkconfig`，日志只记录“configured”，不输出密钥。USB 串口可用 `/agent <文本>` 做不录音的对话冒烟测试。
- `main/volcengine_voice.c` 保留火山 ASR、双向 TTS 和既有音频队列，将 ASR 最终文本改为提交给 Agent，收到 Agent 最终回复后才进入 TTS。按键打断、网络断开、超时和 Agent 错误均会释放活动请求，避免阻塞下一轮。
- ESP-IDF 5.5.3 全量重新配置与 Ninja 构建通过；ESP-Claw Core 在当前 SDK 兼容，无需安装 5.5.4。镜像为 `0x2714b0`，15 MiB factory 分区余量 `0xd7eb50`（85%）。待烧录后先验证 Agent 初始化、串口文本对话，再验证右键“ASR -> Agent -> TTS”闭环。
- 串口调试修正为直接读取 USB Serial/JTAG（标准输入仍指向 UART0）。修正后镜像 `0x2722a0` 已烧录 ESP32-P4 v1.3，bootloader、分区表和应用镜像均通过设备端哈希校验；启动日志确认 `claw_core_agent_1` 初始化和 Agent task 已启动，`/agent <文本>` 已被正确解析。主机侧同一私有配置的 DeepSeek 请求返回 HTTP 200。现场 C6 Wi-Fi 当前持续断开（reason 201，未获取 IP），所以设备请求按预期拒绝，待热点恢复后再完成串口真实对话与右键 ASR -> Agent -> TTS 验收。
