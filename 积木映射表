从用户的视角来看，目前四行四列是这样排序的，以此为基准。
	    第一列     第二列     第三列     第四列
第一行   4-7      3-5      2-3      1-1
第二行   4-8      3-6      2-4      1-2
第三行   5-9      6-11     7-13     8-15
第四行   5-10     6-12     7-14     8-16

例如3-5，3的意思是这是级联的第三块底板模块，5的意思是，从拓展板到底板级联出去的信号顺序来查数，这个是第五个槽位。具体可以看底板模块的原理图网表。
而第1、2、3、4底板模块分别对应0x70地址的i2c拓展模块板子的1、2、3、4接口，也就是分别对应1-8通道。第5、6、7、8底板模块分别对应0x71地址的i2c拓展模块板子的1、2、3、4接口，也就是分别对应1-8通道。具体可以看i2c拓展模块的原理图网表。

从四行四列的那个图并结合原理图网表可以看出，我的底板模块是竖着排起来的，且所有底板模块的方向都一致，都是上面的2x10pin接口输入，下面的2x10pin接口输出。

下面是具体的每个底板模块中的红外接口的定义。每个底板模块有两个槽位，每个槽位有4个端口，一个底板模块共8个端口。
按照我刚刚和所的实际排列的底板模块的方向，对于单个底板模块，为方便说明将上面的槽位命名为槽位1,槽位1周围的是端口0-3，下面的是槽位2,其周围是端口4-7。这里的端口号，和底板模块中我对端口飞线网络及元件位号的命名是一致的。从用户的视角来看，槽位1上下左右对应的端口号分别是3、1、2、0，槽位2上下左右对应的端口号是5、7、4、6。

接下来规定各个门电路端口的对应。非门左侧端口输入，右侧端口输出；输入模块四个端口全部等价，均作为输出端口；输出模块左侧端口输入；其他门电路都是两输入一输出的，上面的输入对应上端口，下面的输入对应下端口，输出对应右端口。对于没有提到的端口，如果连线的话，底板正常亮灯，但屏幕上不显示，也就是和两端没有连到门电路的端口的线一个处理逻辑。为了保证端口对应准确，需要先判断此积木的槽位对应的是单个底板模块的槽位1还是槽位2，之后才能正确判断端口号和上下左右的对应关系。
